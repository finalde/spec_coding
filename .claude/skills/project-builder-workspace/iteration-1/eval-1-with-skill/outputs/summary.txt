## Summary: data_fetcher project scaffold

### Task
Scaffold a new project called `data_fetcher` under `projects/` that fetches data from a REST API and saves it as JSON. Main class: `Fetcher`. No dependencies.

### Skill followed
.claude/skills/project-builder/SKILL.md

### Files created

- projects/data_fetcher/main.py
  Thin CLI entry point (~15 lines). Parses --url and --output arguments, instantiates Fetcher, and calls runner.run(). No business logic.

- projects/data_fetcher/requirements.txt
  Empty project dependencies file (no dependencies specified).

- projects/data_fetcher/libs/__init__.py
  Empty package marker file.

- projects/data_fetcher/libs/fetcher.py
  Typed skeleton class `Fetcher` with:
    - __init__(self, url: str, output_path: str) -> None
    - fetch(self) -> dict   (placeholder, body: pass)
    - save(self, data: dict) -> None   (placeholder, body: pass)
    - run(self) -> None   (orchestrates fetch + save)

- projects/data_fetcher/README.md
  Project documentation with usage examples, project structure tree, and development instructions.

- requirements.txt (updated)
  Root requirements.txt updated to include: -r projects/data_fetcher/requirements.txt

### Notes
- `make` was not available in the environment; the `make new-project` scaffolding effect was replicated manually.
- All CLAUDE.md conventions followed: thin entry point, typed class skeleton, OO design, str | None union syntax ready, README included, registered in root requirements.txt.
