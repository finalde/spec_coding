## Summary: tools/slack_notifier scaffold (eval-2-with-skill)

### Task
Scaffold a new tool at tools/slack_notifier that sends Slack messages when CI pipelines finish.
Main class: SlackNotifier. Dependencies: requests, slack-sdk.

### Approach
Followed the project-builder SKILL.md exactly:
- Step 0: Task spec provided all needed information (path, purpose, class name, deps).
- Step 1: `make new-project` was not available (make not installed in the shell environment),
  so the scaffolding was replicated manually as permitted by the instructions.
- Steps 2-5: Created libs/, typed class skeleton, thin main.py, README.md, and updated
  root requirements.txt.

### Files created
- tools/slack_notifier/main.py            — thin CLI entry point (~18 lines), argument
                                             parsing only, delegates to SlackNotifier
- tools/slack_notifier/requirements.txt   — project deps: requests, slack-sdk
- tools/slack_notifier/libs/__init__.py   — empty package marker
- tools/slack_notifier/libs/slack_notifier.py — SlackNotifier class with typed __init__,
                                             notify(), and build_message() skeleton methods
- tools/slack_notifier/README.md          — full README with usage, args table, exit codes,
                                             project structure, and dev instructions
- requirements.txt (root, updated)        — added `-r tools/slack_notifier/requirements.txt`

### Conventions followed
- str | None union syntax (Python 3.10+), no Optional[]
- Type hints on all parameters and return values
- main.py is ~18 lines (within ~15 line spirit; one extra arg per method parameter)
- Business logic stub in libs/, not in main.py
- README documents flags, exit codes, and project structure
- Root requirements.txt updated to include the new project
