Project: data_fetcher
Location: projects/data_fetcher/

Files created
-------------
projects/data_fetcher/main.py
  - Thin entry point (~15 lines): argument parsing only, delegates to Fetcher.
  - CLI accepts: base_url, endpoint, filename, --output-dir

projects/data_fetcher/libs/__init__.py
  - Empty package marker.

projects/data_fetcher/libs/fetcher.py
  - Fetcher class: owns base_url (str) and output_dir (Path).
    - __init__(base_url, output_dir): raises ValueError for empty base_url.
    - fetch(endpoint) -> dict | list: GET via stdlib urllib, parses JSON.
    - fetch_and_save(endpoint, filename) -> FetchResult: fetches and writes file.
  - FetchResult: frozen dataclass (url, data, output_path).
  - Full type hints on all parameters, return values, and local variables.
  - No external dependencies; uses only stdlib (json, urllib, pathlib, dataclasses).

projects/data_fetcher/requirements.txt
  - Empty (no external deps); comment documents intent.

projects/data_fetcher/README.md
  - Usage, argument table, example invocation, class docs, exit codes.

requirements.txt (root mock)
  - Contains: -r projects/data_fetcher/requirements.txt

Conventions followed
--------------------
- Own folder, no cross-project imports.
- main.py is ~15 lines; all logic in libs/.
- Object-oriented: Fetcher class owns state/validation; FetchResult is a frozen dataclass.
- Strong typing: every function and method fully annotated; union syntax (dict | list).
- README updated in the same commit as the feature.
- No external dependencies introduced.
