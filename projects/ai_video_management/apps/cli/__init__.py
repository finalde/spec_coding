"""CLI entry points for ai_video_management.

Per follow-up 096: the novel-downloader is CLI-only; the API exposes only
read endpoints (GET /api/novels). Run with:

    python -m apps.cli.novel_download

from the project root.
"""
