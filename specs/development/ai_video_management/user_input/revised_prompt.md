# Revised prompt — ai_video_management

**task_type:** development
**task_name:** ai_video_management
**Composed from:** `raw_prompt.md` + every `follow_ups/*.md` in numerical order (001–082 prior; **083** escalates the full-body framing from a single 镜头 prefix line (081) to a **triple anchor** distributed across prompt prefix + middle + pre-tail positions. Three new module-level constants in `actor__chinese_prompt.py` — `_LEADING_FRAMING_MANDATE` (line 0, with `【强制 MANDATORY · 全身从头到脚】` prefix + EN duplicate `MUST show entire body from top of head to toes`), `_RESTATE_FRAMING_MANDATE` (line 1, 10-waypoint anatomy checklist ① 头顶（含发丝）→ ② 面部 → ③ 颈 → ... → ⑩ 脚趾 + `生成任何 portrait / 半身 / 特写 / 头肩 / 腰上 / 胸上 构图 = 生成失败` failure semantics), `_TAIL_FRAMING_MANDATE` (penultimate line, `整图必须 9:16 竖屏 + 头顶到脚趾 zero-crop + 头部仅占画面 1/5 + 身体占 4/5 + 头顶留 ~5% 顶边 + 脚趾下方留 ~5% 底边 + 如未满足任何一项均视为不合格`). All 4 builder variants (`build_face_prompt`, `build_body_prompt`, `build_face_prompt_with_picks`, `build_body_prompt_with_picks`) consume the same constants so legacy and batch-coordinated paths stay byte-aligned. The opening descriptor line gains a `**【强制全身】**` markdown-bold prefix as a 4th anchor in the high-attention prefix band. 081's single leading 镜头 line + 082's identical line in picks-builders both superseded — no backward-compat shim. Smoke: all 4 builders carry lead+restate+descriptor+tail anchors verbatim ✓; pytest 15 pass / 3 pre-existing wukong fixture failures (= 082 baseline, 0 regressions). HTTP routes + JSON shapes byte-identical. See `follow_ups/083-20260517-220313-mandatory-full-body-triple-anchor.md`. Prior 082 adds within-batch face/body-pool diversity for actor preview + generate: for every of the 7 pools (eyes / nose / lips / brow / contour / skin / body) no two slots in the same batch share the same descriptor unless the pool is genuinely exhausted (count > pool_len). New `_batch_sample_pool` helper implements bias-preferred + exhaust-then-fall-through + 074's 25% wild-card retained at batch level. New `_resolve_batch_picks(batch_seed, batch_size, slot_index, look, archetype)` resolves a slot's 7 pool draws deterministically — same `batch_seed` across N parallel `count=1` calls yields the same per-pool sample list, each call picks its own `slot_index`. New `build_face_prompt_with_picks` / `build_body_prompt_with_picks` accept caller-supplied pool picks; 081's framing line + 080's wardrobe + photo cue stay per-slot. `ActorPool` gets a `_build_prompts_for_slot` dispatcher; `preview_prompts` + `generate_batch` gain 3 optional kwargs (`batch_seed/batch_size/slot_index`). CDTO + Protocol + Pydantic body + TS request type all gain the 3 fields. Frontend `ActorPoolGenerator.tsx`: `batchSeed = Date.now()` once per click, passed alongside `slot_index=i` to each parallel call; new `previewBatchRef` carries the metadata into the worker-pool generate so Kling-rendered prompts match preview byte-for-byte. Smoke: batch-coord 10/10 distinct across all 7 pools (count=10, look=sinister); legacy uncoordinated 4/10 eyes + 6/10 body for the same setup (birthday-problem collisions). Bias exhaustion test (cunning, 2-element lips bias, count=10): lips still 10/10 distinct via fall-through. pytest 15 pass / 3 pre-existing wukong fixture failures (= 081 baseline, 0 regressions). HTTP route paths + response shapes byte-identical. Scope-deferred (per user clarification this turn): 6-dropdown random_dim diversity + diverse-mode path. See `follow_ups/082-20260517-203812-batch-pool-diversity.md`. Prior 081 forces every actor generation to render as a full-body head-to-toe photo by relocating the framing decision into the prompt's high-attention prefix: a new leading `镜头：full-body wide shot / long shot, 9:16 竖屏构图, 头顶到脚趾完整入框, 头部上方 ~5% 顶边, 双脚下方 ~5% 底边, 严禁任何 portrait crop / head-shoulder framing / 半身像 / close-up。` line is prepended to both `build_face_prompt` and `build_body_prompt`. The opening "正面全身定妆照（头部对焦）..." had been silently biasing Kling toward head-emphasis — replaced with "正面**全身**定妆照（远景 wide / long shot; 头到脚完整入框; 头部清晰仅用于身份识别, 不主导构图）". 姿态 lines drop `头部对焦清晰` → `头部清晰可辨` and append `双手 + 双脚 完整入框不可被裁切`. 画面 lines tighten to numeric `9:16 竖屏 / 头顶到脚趾完整可见 / 头部上方 ~5% 顶边 / 双脚下方 ~5% 底边 / 头部占画面上 1/5 (留 4/5 给身体)`. `_NEGATIVES_ZH` escalates to four imperative `**严禁**` clauses covering portrait crops, head > 1/4, body < 70%, and hand/foot edge bleed. No HTTP / JSON / endpoint changes. See `follow_ups/081-20260517-202625-actor-force-full-body-framing.md`. Prior 080 upgrades the actor casting comp-card wardrobe from the 076-era tank-top + booty-shorts to industry **swimwear-standard** (运动比基尼 / 赤膊 + 高叉紧身短) so the single comp-card frame exposes every body metric the user named — 胖瘦 / 腿长短 / 腿型直弯 / 大腿内外侧线条 / 胸大胸小 / 胸型 / 肩宽 / 腰线 / 腰臀比 / 臀型 / 上身肌肉线条. `_casting_wardrobe('女性')` and `('男性')` rewritten with explicit minimum-coverage descriptors; `_CASTING_REQUIREMENTS_ZH` enumerates the 11-metric body-readability list and tightens the framing requirement 头顶到脚踝 → 头顶到脚趾 + 一帧定格不裁切 (脚踝 still allowed feet to crop); `build_face_prompt` + `build_body_prompt` 画面 lines align with the same toe-inclusive framing; `_NEGATIVES_ZH` adds three guards — (a) cropped feet / thighs / half-body / head-shoulder framing failures, (b) modest fallback wardrobe (T-shirt / long pants / coat / any 遮挡躯干或大腿轮廓的服装), (c) glamour-pose drift (媚态 / 内衣广告感) since minimal wardrobe is a body-shape evaluation tool not glamour. Pose unchanged (自然站立 + 双臂略外开 15° 中性站姿). No HTTP / JSON / endpoint changes. Slot 079 was taken by a parallel "look-led-archetype-classification" follow-up; this one occupies 080. See `follow_ups/080-20260517-200256-actor-minimal-wardrobe-full-body.md`. Prior 079 (look-led classifier) fixes the sinister-female → everyman fall-through regression by making `_classify_actor_attrs` a 4-priority look-led router + cross-gendering follow-up 064's 5 look→archetype mapping so every (look × gender) combo has an anchoring archetype — see `follow_ups/079-20260517-113948-look-led-archetype-classification.md`. Prior 078 bumps character reference turntable from 2.9s to 4s (Seedance upload ceiling relaxed in 2026-05) + locks a "first 2s self-sufficient" contract: in 0-2s the character must finish saying Chinese 一 + 二 with the 360° pass completing and camera returning to front, so the existing `ShotConcatBuilder._ffmpeg_concat` `_CONCAT_SEGMENT_S = 2.0` trim and the ✂ 截到 2s button (`_TRUNCATE_DURATION_S = 2.0`) continue to land on meaningful content downstream; 2-3s is recompiled as 推 + 三; 3-4s is a new 1s face close-up freeze as the face-viewer final lock. Updates `.claude/agent_refs/project/ai_video.md` rule #12.5 v4 → v5 (cross-cutting for all ai_video task_type projects) and ripples to mozun_chongsheng's 10 character files via sibling follow-up `specs/ai_video/mozun_chongsheng/user_input/follow_ups/019-…-character-ref-4s.md`. **No code change** to `character_video__writer.py` — the existing 2s trim path is already the canonical contract; follow-up 078 just makes the upstream prompts feed it useful first-2s frames. Slot 076 deliberately skipped due to orphan reference in 077's changelog entry. See `follow_ups/078-20260517-192826-character-ref-4s-truncate-2s.md`. Prior 077 makes the user-selected `look` value (especially the 5 character-archetype values righteous / sinister / seductive / cunning / innocent) **dominate the overall vibe** of the generated actor preview prompts by adding `_LOOK_FEATURE_BIAS_ZH` 子集 to the 5 五官 + 轮廓 + 体型 pools AND appending an explicit `气质：...` overlay line right after `综合描述`; 8 物理 look 值保持 byte-identical (`_pick_biased` 退化为 uniform 当 bias dict 为空) — see `follow_ups/077-20260517-192220-look-dominates-feature-bias.md`. Prior 075 switches all Kling actor-gen prompts from English variance-composed strings to **structured Chinese** (眼睛/鼻子/嘴巴/眉毛/轮廓/皮肤/体型 + 综合描述 + 服装/摄影/避免); new `libs/infrastructure/writers/actor__chinese_prompt.py` per SRP holds 7 Chinese pools + 10-archetype synthesis map + 体型-bias map; `ActorPool._build_face_prompt` + `_build_body_prompt` signatures change to `(attrs, seed, archetype)` and delegate — see `follow_ups/075-20260517-175100-chinese-structured-prompt.md`)

**Last regenerated:** 2026-05-23 12:37:01 — header bump for follow-up 110 (add 5 popular 仙侠 entries to `CANONICAL_NOVELS` in `libs/domain/value_objects/novel__valueobject.py` — 剑来 (id 287, 烽火戏诸侯) / 仙逆 (410, 耳根) / 大奉打更人 (405, 卖报小郎君) / 赤心巡天 (56, 情何以甚) / 大道争锋 (435, 误道者). Source: sudugu.org `/paihang/xianxia.html` page-1 + page-2 ranking, cross-referenced against current catalog to skip duplicates. Each entry uses single `_sudugu(...)` source (ttkan备源 not in scope, can be added later if sudugu抓不下来 — multi-source spec already exists per follow-up 107). Slugs comply with `slug.replace("_", "").isalnum()` rule. Inserted at the end of the 仙侠 section, before the 玄幻 marker. Background `python -m apps.cli.novel_download --workers 2` task (resume after follow-up 109 jitter + cn.ttkan switch) was already running before catalog amendment; new 5 entries will be picked up on the next downloader launch (no forced restart). No other code changes — `download_all` iterates `CANONICAL_NOVELS` so additions are automatically in scope. See `follow_ups/110-20260523-123701-add-five-popular-xianxia.md`. **Prior bump:** 2026-05-21 20:31:17 — header bump for follow-up 109 (three quick wins extracted from reading `freeok/so-novel`'s declarative source-rule schema — `bundle/rules/*.json` — deliberately scoped tight per user direction; the declarative `SourceRule` refactor + bs4/CSS-selector port + adding `xbiqugu.la`/`22biqu.com`/`shuhaige.net` as 3rd/4th fallback hosts all deferred). **(a) `ttkan.co` → `cn.ttkan.co`** in the `_ttkan()` factory of `libs/domain/value_objects/novel__valueobject.py:65`; so-novel's `bundle/rules/no-search.json` targets the `cn.` subdomain which serves **Simplified Chinese** (fixes follow-up 107's "Traditional Chinese — small content style mismatch" caveat). Live-verified against `https://cn.ttkan.co/novel/chapters/fanrenxiuxianchuan-wangyu`: HTTP 200, 0/8 Traditional char hits + 5/8 Simplified char hits on a sample of common 简↔繁 pairs, 2453 chapter links parse with the existing `_TTKAN_CHAPTER_LINK_RE` byte-identical to `www.ttkan.co`. Chapter-page `<div class="content">` shape unchanged so `_TTKAN_CONTENT_BLOCK_RE` still works. **(b) `(本章完)` end-of-chapter filter** — new module-level `_END_OF_CHAPTER_MARKER_RE = re.compile(r"\(本章完\)")` applied inside `_extract_paragraphs` after the HTML-tag strip and entity decode, before the empty-paragraph drop; so-novel's `bundle/rules/*.json` lists this exact regex under `chapter.filterTxt` on every site rule for both sudugu and ttkan, and the marker was leaking into body `.md` files. **(c) Random jitter delay 1.0–2.0 s** replacing fixed 0.8 s — `_INTER_REQUEST_DELAY = 0.8` split into `_INTER_REQUEST_DELAY_MIN = 1.0` + `_MAX = 2.0` (so-novel's converged anti-bot values for both sudugu and ttkan in `bundle/rules/rate-limit.json`); `NovelDownloader.__init__` signature `delay_seconds: float` → `delay_min_seconds: float, delay_max_seconds: float`; `_http_get` computes `random.uniform(self._delay_min, self._delay_max)` per request; `_download_in_isolated_worker` plumbs both delays to per-worker `NovelDownloader` instances. Per-worker average request rate goes from 1.25 req/s → ~0.67 req/s, peak aggregate ~3.3 req/s at 5 workers — harder to fingerprint than fixed cadence at the same average. CLI doesn't pass either delay (uses defaults), so `apps/cli/novel_download.py` is untouched. **Migration on next launch** for the 5 ttkan-bearing novels (fanren_xiuxian_zhuan / guangyin_zhiwai / wanmei_shijie / ze_tian_ji / lingjing_xingzhe): existing `_meta.json` carries `active_source_host="ttkan.co"`, new spec exposes `cn.ttkan.co` → `source_changed=True` path in `_ensure_index` (introduced in follow-up 107) → ttkan index re-fetched from the `cn.` subdomain → chapter URLs rewritten by `idx` while preserving `done`/`hash`/`error`. Already-downloaded Traditional Chinese fragments in `novels/xianxia/fanren_xiuxian_zhuan/fanren_xiuxian_zhuan.md` stay verbatim (opencc 繁→简 still deferred per 107); new chapters appended as Simplified — mixed-script boundary acknowledged, user can opencc-convert manually if a uniform script is needed. Sudugu paths byte-identical (host unchanged, all 34 sudugu-only novels untouched). Smoke 5/5 PASS: ttkan factory resolves to `cn.ttkan.co`, end-of-chapter regex strips marker leaving surrounding text clean, jitter constants present, constructor accepts new signature, `fanren_xiuxian_zhuan` exposes `[sudugu.org, cn.ttkan.co]` in order. **Explicitly deferred** (would have been path 2/3 in the user's pick menu): declarative `SourceRule` dataclass + `bs4`/`selectolax` CSS-selector engine (so-novel's `rule-template.json5` is genuinely better than per-host regex but the 2–3 hr refactor + replay-test scope is held until a 3rd source is needed); 3rd/4th fallback hosts (`xbiqugu.la` / `22biqu.com` / `shuhaige.net` — would need the refactor first); `@js:` rule support (mini-racer / pyjsdom too heavy for narrow use); cloudflare and proxy-required tier hosts (TLS fingerprint spoofing or actual proxies — out of scope for v1). HTTP routes + JSON shapes + frontend types byte-identical. The Legado scaffolding from 108 (`libs/infrastructure/readers/legado__reader.py` + `sources/ttkan_co.json`) stays unwired and untouched — 109 is a different architectural direction (surgical patches to the existing helpers, not a rule-engine swap). See `follow_ups/109-20260521-203117-cn-ttkan-jitter-end-of-chapter.md`. **Prior bump:** 2026-05-21 00:52:00 — header bump for follow-up 108 (new opt-in **Legado-3.0-rule-driven HTML reader** scaffolding for future novel-source fallbacks; no production download path touched). New files: `libs/infrastructure/daos/legado_source__dao.py` (frozen-dataclass DAO mirroring Legado book-source JSON with `from_legado_json(dict)`), `libs/infrastructure/errors/legado_source__error.py` (`LegadoRuleError` / `LegadoUnsupportedSyntaxError` / `LegadoFetchError`), `libs/infrastructure/readers/legado__reader.py` (stateless `_LegadoEngine` supporting XPath, CSS via `@css:`, default tag/class/id path syntax `class.X@tag.Y@text` with `.N` indexing + `@children[N]` + `@text` / `@href` / `@src` / `@textNodes` accessors, `&&` multi-rule concat, trailing `##pat##rep##` regex replace — rejects `@js:` / JSONPath / AllInOne single-colon regex with `LegadoUnsupportedSyntaxError`; public `LegadoReader` with `fetch_toc` / `fetch_chapter` / `fetch_book_info` + `from_json_file(path)` classmethod, owns its `httpx.Client` with same UA + zh-CN headers as the existing downloader), `libs/infrastructure/readers/sources/ttkan_co.json` (vendored Legado community source for `cn.ttkan.co` from XIU2/Yuedu#85; first data point exercising the rule grammar end-to-end). New deps `lxml>=5.0` + `cssselect>=1.2` added to `projects/ai_video_management/pyproject.toml` + mirrored `requirements.txt` (both ship precompiled Windows wheels for Python 3.10+, no native build chain). **No wiring** into `novel__writer.py`'s production download path: Legado would not fix follow-up 106's anti-bot/speed problem (same HTTP signature trips the same rate limit), so this reader targets follow-up 107's extension axis — future fallback hosts can be added as `sources/{host}.json` book-source files instead of hand-coded `_fetch_index_via_{host}` Python helpers. Existing sudugu/ttkan Python helpers stay untouched (no parity risk). See `follow_ups/108-20260521-005200-legado-rule-reader.md`. **Prior bump:** 2026-05-21 00:24:55 — header bump for follow-up 101 (UI: sidebar tree defaults to **everything collapsed** on initial load). Single-line change in `apps/ui/src/components/Sidebar.tsx`: the tree-load `useEffect` (line 91) now seeds `accum[node.path] = false` instead of `true`, so directories / sections render closed by default. The render-side line 133 `const isOpen = depth === 0 ? true : expanded[node.path] === true;` is unchanged — top-level sections (currently just "AI Videos") still auto-show because they have no collapse affordance. The currentPath auto-expand effect (lines 98-108) is preserved verbatim, so deep-linking to `/file/<path>` still walks ancestors open. The `{ ...accum, ...prev }` merge order is unchanged, so user-toggled expansions persist across tree refreshes triggered by create / delete / rename. No new persistence layer (default-collapsed re-applies on every page load). No change to `Reader`, `Breadcrumb`, or any other component. See `follow_ups/101-20260521-002455-sidebar-default-collapsed.md`. **Prior bump:** 2026-05-20 21:33:34 — header bump for follow-up 100 (UI: make Reader breadcrumb segments clickable so each parent path component navigates to its corresponding ancestor). Pure frontend change in `apps/ui/src/components/Breadcrumb.tsx` + matching styles in `apps/ui/src/styles.css`. Breadcrumb gains a `knownPaths` prop + an `onNavigate(path)` callback; each non-last segment renders as a `<button class="breadcrumb-link">` that, when activated, prefers `<accumulated-prefix>/<segment>.md` from `knownPaths` (covers the self-named-md convention used by `actor_<id>/actor_<id>.md` and `c<n>_<slug>/c<n>_<slug>.md`) and falls back to `<accumulated-prefix>` itself. `Reader.tsx` wires `knownPaths` through from `App.tsx` (already passed to Reader) and supplies `onNavigate={(p) => navigate("/file/" + encodeURIComponent(p))}` reusing the existing react-router navigation pattern. Last segment keeps the existing `breadcrumb-current` non-interactive styling. No backend / API / route changes; no new folder-browsing route; the existing `Sidebar` `currentPath` effect already auto-expands the tree when navigation lands on a folder-shaped path. See `follow_ups/100-20260520-213334-clickable-breadcrumb-navigation.md`. **Prior bump:** 2026-05-19 20:22:33 — header bump for follow-up 099 (character ref rule #12.5 v10.2 → v11: **simplified prompt language, camera motion described ONCE only**). Schedule unchanged (3 static landings at 0°/90°/180° + 2 short transitions, same `CANONICAL_VIEWS (1.0, 3.5, 6.0)` timestamps — no code change). v10.2 failure: user empirical test of v10.2 renders showed motion started at ~5s (vs spec 2s) because the prompt described the camera path in 4 different fields (镜头 + 动作 + 节奏 + 负向 qualifiers) with technical jargon ("motion bridge" / "static landing" / "locked-framing"). Model averaged across redundant descriptions and under-committed to motion. User directive: "tell it in a more simple way and only once in the prompt". v11 fix: motion ONLY described in 动作 timed beats field, using plain Chinese ("镜头围绕角色顺时针绕 90° 到角色左侧身" / "镜头停在左侧身角度不动"). 镜头 = framing/lens only, no motion path. 节奏 = one sentence ("单 take 7s, 角色站立不动只说话, 镜头按 动作 timed beats 旋转 + 停顿"). 负向 = 10 simple bans (no qualifier paragraphs). 锁定机位 jargon removed (model interprets "锁定" as "全程不要动"). **Cross-cutting rule patch**: `.claude/agent_refs/project/ai_video.md` rule #12.5 v10.2 → v11 — v10.2 verbose multi-field prompt demoted to archive with explicit `"为什么 v10.2 verbose multi-field prompt 不再生效"` rationale (model under-commits to motion under 4-field redundancy); v11 active section + 镜头/动作/节奏/负向/file-note/h1/title/table-heading/table-用途 all rewritten for plain Chinese single-source-of-truth structure; footer attribution appended. **No code change** — `CANONICAL_VIEWS` constants `(1.0, 3.5, 6.0)` unchanged; backend writer / endpoint / frontend button all consume the tuple opaquely. **项目落地**: 10 character md files patched via two-script sequence (`patch_chars_v11.py` for 12 substitutions / file covering 文件说明 + h1 + table heading + 3 table 用途 rows + 场景 + 镜头 + 节奏 + 负向 (V10.2 block → v11 block) + 动作 6-line block (regex preserving character name + 标志台词 #1/#2) + 台词 enumeration block; then `patch_chars_v11_fix.py` for the title-line regex + 光线 line regex which used different wording in files vs the rule template). All 10 confirmed at v11, zero v10.2 motion-jargon markers remaining. Per-character 标志台词 preserved verbatim — slot 4 now appears ONCE in the 动作 5-7s beat (combined with slot 5 in the same line), simpler than v10.2's split-across-3-beats. Title line in code block now reads `{NAME} · {ROLE} — 角色 reference 7s 单 take`. 渲染样式 untouched (character-specific style adders are valuable, leave as v10.2 form). **Hypothesis**: motion described ONCE + plain Chinese will let the model trust the spec instead of averaging across 4 conflicting descriptions. **Risk**: if v11 simplification still leaves motion delayed, the issue is a fundamental model bias (not prompt redundancy) and retreat is v12 (shift schedule earlier, break 0-2s truncate-compat, CANONICAL_VIEWS side 0.5/3.5/6.0) or v13 multi-clip (render 3 separate static clips + concatenate). **User-side action**: re-render the 10 character mp4s with v11 prompt — v10 + v10.2 renders are invalidated. See `follow_ups/099-20260519-202233-character-ref-v11-simplified-prompt.md`. **Prior bump:** 2026-05-19 00:06:05 — header bump for follow-up 098 (character ref turntable rule #12.5 v10 → v10.2: replace v10's single 4s continuous 180° orbit with v10.2's **3 static landings + 2 short motion bridges**, after empirical evidence the model under-rotates v10's continuous orbit). v10's "single continuous slow orbit at 45°/s" gave the model too much latitude — user reported after first v10 renders that "the side is still almost front, the back picture actually shows side ... the video does not have a backview in it, I think it start to move around 4~5s so the last frame in the video is still side view". Diagnosis: video models don't honor timed-beat *speed* instructions for "slow continuous motion" — they interpret slow relative to internal pacing, with ease-in/ease-out at boundaries + under-rotation tendency in short clips. The math `(t-2)×45°/s = exact angle` is correct for the spec, but the rendered video doesn't follow linearly. Model rendered at ~22°/s (half spec) with motion starting ~4-5s — 7s video never reaches 180°. **v10.2 architecture**: instead of asking the model to estimate orbit progress across 4 seconds, give it **explicit static targets** to land at — 5-phase schedule = 0-2s static front + 2-3s motion bridge 0°→90° + 3-4s static side hold + 4-5s motion bridge 90°→180° + 5-7s static back hold + settle. Each motion bridge is 1s with explicit terminal angle (90° at t=3s, 180° at t=5s). Model can pace each motion bridge however it likes — the static landings at the end of each bridge are the contract. **Angle contract changes from "time × speed" to "explicit landing angle hold".** Extract picks now all anchored on static lock windows: front t=1.0s (mid 0-2s static, unchanged), side t=4.0s → **t=3.5s** (mid 3-4s static side; was falling in motion segment in v10), back t=6.0s (mid 5-7s static back; was falling in motion tail in v10). **Code change**: `projects/ai_video_management/libs/domain/value_objects/character_video__valueobject.py` `CANONICAL_VIEWS` side timestamp 4.0 → 3.5; back timestamp 6.0 unchanged (same absolute time but now reliably lands in 2s static window 1s removed from motion-end at t=5s). Module docstring updated to reference v10.2's 5-phase camera path + algebraic derivation from static-landing schedule. **Cross-cutting rule patch**: `.claude/agent_refs/project/ai_video.md` rule #12.5 v10 → v10.2 — 3-phase template → 5-phase template; v10 demoted to archive with explicit `"为什么 v10 的 4s 连续 orbit 不再生效"` rationale paragraph documenting the empirical under-rotation; 节奏 = "锁定 framing 5-phase 单 take, 3 static landings + 2 motion bridges" (was "锁定 framing 单方向慢速 orbit 7s 单 take"); 镜头/动作/台词/负向/locked-fields list all rewritten for 5-phase; negatives swap (drop v10's `不要 mid-shot freeze (除 0-2s + 6-7s 外, 2-6s 段全程匀速运动)` which directly conflicts with v10.2's explicit mid-shot statics + add `不要 motion 跨越目标角度 (1s motion bridge 必须精确终止在 90°/180°)` + `不要 静态段内继续微调机位 (3-4s 段 + 5-7s 段必须完全静止)` + modify existing quals for motion speed + cut/transition + motion blur). 14 video-specific negatives total (was 13 in v10). **项目落地**: 10 character md files (`ai_videos/mozun_chongsheng/characters/c{1..10}_*/c{N}_*.md`) patched via two one-shot Python scripts (`patch_chars_v10_2.py` for 13 fixed-string + 4 regex changes per file, then a corrective `patch_chars_v10_2_fix.py` to split v10's combined 3-5s+5-7s beat block into v10.2's 3-4s + 4-5s + 5-7s after the first script's multi-line regex missed because v10 file content used 2-beat instead of 3-beat structure). 10/10 confirmed at v10.2, zero v10 markers remaining. Per-character 标志台词 #1 + #2 preserved verbatim — slot 4 dialogue now spans static-side hold + motion-to-back (delivered across the 3-4s static + 4-5s motion transition), slot 5 fully sits in 5-7s static back settle. **Risk acknowledged**: v10.2 is hypothesis (bookended motion segments with 0 velocity at landing boundaries ≠ v6 whip-pans; if v10 passed Kling validator, v10.2 should pass since motion total time is shorter). Retreat paths: v10.3 (drop one motion bridge — 0-2s front + 2-3s motion + 3-7s side hold, loses back angle), v10.4 (drop both bridges — 7s static front = v8 + v10.2 negatives, loses side + back), v11+ multi-clip (3 separate 2-3s clips concatenated, most expensive most bulletproof). **2s truncate-compat preserved across v8/v9/v10/v10.2** (0-2s content byte-identical: static + frontal + 一/二). See `follow_ups/098-20260519-000605-character-ref-v10.2-static-landings.md`. **Prior bump:** 2026-05-18 23:57:49 — header bump for follow-up 097 (frontend dual-placement parity fix: add the 🖼 "提取三视图+音频" button to the direct-video-view page in `Reader.tsx`, mirroring the existing 🎞 "Extract Frames" button which already had dual placement (per follow-up 062). The 🖼 button was wired by follow-up 093 only to per-tile thumbnails inside the `SiblingMedia.tsx` panel, which renders below md / shot-pair / image-ref files — so a user navigating directly to a character mp4 via the file tree could not reach the extract feature without first navigating to the character `.md` file and scrolling past the markdown content). Two-file change: (1) `apps/ui/src/components/SiblingMedia.tsx` — promote `isCharacterVideoPath` from module-private to named export so Reader can reuse the path-shape gate without duplicating the regex; single `function` → `export function` change, no behavior change in SiblingMedia itself. (2) `apps/ui/src/components/Reader.tsx` — add `extractCharacterViews` to api imports + `isCharacterVideoPath` to SiblingMedia imports, new `extractingViews` boolean state parallel to existing `extracting`, new `onExtractCharacterViewsClick` handler parallel to `onExtractFramesClick` (calls `extractCharacterViews(path)`, success toast `Extracted N views + audio from ${name} → views/` mirrors SiblingMedia wording, failure toast `Extract views failed: ${kind}`, triggers `onSaved()` to refresh the tree so the new `views/` subfolder appears), new `viewsExtractLabel` + `showViewsBtn` derived values, `mediaActionsBusy` extended to include `extractingViews` so all 4 mutually-blocking actions (archive / delete / extract-frames / extract-views) disable each other, new button render block placed between the 🎞 and 📦 buttons inside the `<div className="reader-media-actions">` block, gated by `showViewsBtn = isVideo && !isArchivedFile && !isDeletedFile && isCharacterVideoPath(path)` matching the SiblingMedia per-tile gate. Tooltip references v10 wording (`适用于 v10 character turntable (7s locked-framing + 180° slow orbit)`) since 096 just bumped the spec; SiblingMedia's older tooltip still says v9 (acknowledged in 097's "out of scope" as a separate cleanup not bundled to keep the diff focused on dual-placement parity). No new endpoint / route / Cdto / mapper changes — the wire-up already exists from follow-up 093. Pure frontend dual-placement parity fix. TypeScript check clean on both touched files (pre-existing vite.config.ts errors unrelated). See `follow_ups/097-20260518-235749-extract-views-button-on-direct-mp4-page.md`. **Prior bump:** 2026-05-18 22:40:47 — header bump for follow-up 096 (character ref turntable rule #12.5 v9 → v10: turntable 时长 15s → 7s + **locked-framing single continuous take, no dolly + no zoom**, optimized bottom-up for the extract-3-views + audio pipeline introduced by follow-up 093). v9's 15s slow-push-in + slow-orbit + reverse-dolly design hit a structural snag after 093 went live: the 3 angle picks (front t=1.0s / side t=7.0s / back t=9.0s) landed at different framings because v9's dolly-in + reverse-dolly varies the head-size-in-frame across the take — front pick lands at wide, side pick lands at head-1/3-frame mid-pull-back, back pick lands closer-to-wide but still mid-pull-back — so the 3 extracted pngs cannot be used as a coherent 3-view character sheet for downstream image-to-video models. v10 reverses v9's mixed-framing: drop the 2-5s dolly-in (accept medium-full ~40mm ≈ head 1/5 frame ≈ 360-400px tall at 9:16 1080p instead of v9's dedicated face MCU at 1/3 frame), drop the 5-13s reverse-dolly (camera distance to character locked throughout), 360° orbit → 180° orbit (body is bilaterally symmetric, 270° right-side angle is redundant with 90° left-side), 15s → 7s. 3-phase camera path: 0-2s static front lock (medium-full, 一+二, byte-identical to v8/v9 content) + 2-6s slow ccw 180° orbit at 45°/s (locked distance, no dolly, no zoom) + 6-7s static back lock (180° angle, same framing). Dialogue table 5-row preserved, slot 3 (2-3s) / 4 (3-5s) / 5 (5-7s) retimed for v10's 3 phases. Negatives bumped from v9's 11 items to v10's 13 items (added explicit no-dolly + no-zoom + no-framing-change bans). **Code change**: `projects/ai_video_management/libs/domain/value_objects/character_video__valueobject.py` `CANONICAL_VIEWS` tuple side timestamp 7.0 → 4.0 (= 90° at (4.0-2.0)×45°/s = exactly 90°), back timestamp 9.0 → 6.0 (= 180° at (6.0-2.0)×45°/s = exactly 180°). front timestamp 1.0 unchanged (still mid 0-2s static intro). Module docstring updated to reference v10's 3-phase camera path. **No other webapp code changes** — the extract route + button + path-gate + Cdto / Cdto-mapper / domain-error / infra-error wiring from 093 all work unchanged. 2s truncate-compat preserved (0-2s content byte-identical across v8/v9/v10; only the framing is fractionally tighter from wide ~35mm to medium-full ~40mm in v10). **Cross-cutting rule patch**: `.claude/agent_refs/project/ai_video.md` rule #12.5 v9 → v10 — 5-phase mixed-framing template → 3-phase locked-framing template; v9 demoted to archive with explicit "为什么 v9 的 15s mixed-framing single-take 不再生效" rationale paragraph; 节奏 = "锁定 framing 单方向慢速 orbit 7s 单 take" (was "缓慢连续运镜 15s with dolly"); 时长 15s → 7s; 镜头 / 动作 / 台词 / 负向 / locked-fields list all retimed and rewritten. **Risk acknowledged**: v10 is still a hypothesis about Kling's validator; if 7s slow-orbit uploads are rejected, retreat to v10.1 (drop orbit entirely, 7s static front — extract pipeline degrades to front-only) or v10.2 (insert ~0.3s static holds at 90° and 180° to break the validator's "continuous motion" pass criterion). **Pre-v10 mp4s already rendered in `ai_videos/mozun_chongsheng/characters/c*` won't extract cleanly at new timestamps** — the t=4.0s and t=6.0s timestamps assume v10's 3-phase schedule; on v9 sources they land in the dolly-in window and reverse-dolly window respectively (not at clean 90° / 180° angles). Mitigation: re-render character refs to v10 before extracting. **No sibling mozun_chongsheng character md ripple in this turn** — same pattern as 092 + 091 + 088 + 078, the user runs the patch script after reviewing this draft. See `follow_ups/096-20260518-224047-character-ref-7s-locked-framing-3view-extract.md`. **Prior bump:** 2026-05-18 22:30:27 — header bump for follow-up 095 (two-fer: **bug fix — batch male generation yields ~half female actors** because the 7 Chinese descriptor pools in `actor__chinese_prompt.py` carry feminine markers like `少女`/`美人`/`闺秀`/`妩媚`/`妖艳`/`婴儿肥` and masculine markers like `男性化`/`邻家男孩`/`阳光男孩`/`健壮型男`/`长腿欧巴` but `_pick_biased` / `_resolve_batch_picks` draw uniformly with no gender filter — a male batch pulls a feminine descriptor in ~30-45% of slots per pool, accumulating to >95% chance of at least one cross-gender leak across 7 attributes, which is enough to push Kling toward female rendering. **UX change — clicking an actor tile in the grid now redirects to the actor sidecar md (`ai_videos/_actors/{id}/{id}.md` → `ActorView` via Reader path-shape detection) instead of the raw jpg viewer** — the jpg is already the tile thumbnail, the md is the real "actor page" with attribute table + assignments + delete controls). Implementation: (a) two new module-level marker tuples `_FEMALE_ONLY_MARKERS` (18 entries) + `_MALE_ONLY_MARKERS` (10 entries) listing only **unambiguous identity terms** (not borderline-feminine physical descriptors like 性感/丰满, not borderline-masculine physical descriptors like 肌肉/肩宽 — those can apply across genders); (b) new helper `_filter_pool_by_gender(pool, bias_indices, gender_slug) -> (filtered_pool, translated_bias)` substring-matches markers, returns the filtered pool + a remapped bias-index tuple (original-index → new-index dict, dropping biases whose source entry was stripped); (c) wired into all 3 call sites — `build_face_prompt` + `build_body_prompt` filter 7 pools before `_pick_biased`/`_pick`; `_resolve_batch_picks` gains a `gender_slug` parameter and filters each pool before `_batch_sample_pool`; (d) `actor__writer.py:2080` passes `gender_slug=attrs.gender` to `_resolve_batch_picks`. Verification: 10-prompt male batch + 10-prompt female batch both show ZERO cross-gender marker leaks (was ~30-45% per pool before). Boot smoke 7/7 pass. UX: `ActorGrid.tsx::onTileClick` derives `mdPath = ai_videos/_actors/${actorId}/${actorId}.md` and `navigate("/file/" + encodeURIComponent(mdPath))`; Reader's existing `^ai_videos/_actors/actor_[^/]+/actor_[^/]+\.md$` shape detector routes to ActorView. **Risk acknowledged**: marker-substring filter is the 80/20 fix; some residual feminizing is possible from non-marker descriptors (e.g., "高颧骨, 立体感强, 模特脸" is gender-neutral on paper but Kling could still skew). If empirical Kling output remains skewed >10% post-fix, fallback options recorded in spec: (a) expand marker lists, (b) introduce gender-specific sub-pools, (c) repeat the `性别:` line later in the prompt. See `follow_ups/095-20260518-223027-gender-bleed-fix-and-actor-tile-redirect.md`. **Prior bump:** 2026-05-18 22:24:55 — header bump for follow-up 094 (add 外貌气质 / `look` as the 5th filter dropdown on `ActorGrid`, parallel to the existing 民族 / 性别 / 年龄段 / 分配状态 filters). Frontend-only change in `apps/ui/src/components/ActorGrid.tsx`: new `filterLook` state, 5th predicate in `filteredActors` useMemo, dependency added to page-reset useEffect, new `<select>` populated from `ATTR_OPTIONS.look` (13 canonical values per follow-up 064: 8 physical-appearance + 5 character-archetype). Dropdown follows existing pattern — Chinese label, English-slug option values matching the per-tile chip display. No backend / API / type changes (`actor.look` already in `ActorInfo`, options already in `ATTR_OPTIONS`). See `follow_ups/094-20260518-222455-actor-grid-look-filter.md`. **Prior bump:** 2026-05-18 20:39:39 — header bump for follow-up 093 (new feature: extract 3 angle views — front / side / back — + the full audio track from a v9 character turntable mp4, outputs land in `{char_folder}/views/`). New endpoint `POST /api/extract-character-views` plus a per-tile 🖼 "提取三视图+音频" button on character-video tiles in SiblingMedia (gated by path regex `^ai_videos/{drama}/characters/c\d+(_…)?/`). Timestamps are anchored to v9's 5-phase camera path: front=1.0s (mid 0-2s static intro), side=7.0s (25% / 90° into the 5-13s slow orbit), back=9.0s (50% / 180° into the orbit). Output naming uses parent-dir-name prefix so re-extraction overwrites: `{char_folder_name}_{front,side,back}.png` + `{char_folder_name}_audio.mp3` (libmp3lame VBR ~165kbps, full source duration). Idempotent: `views/` is mkdir'd + swept of stale `*.png` / `*.mp3` on every run. Partial-failure semantics mirror the existing 8-frame scene extractor: failures accumulate in a `failures` tuple, the route returns 200 unless ALL 4 outputs fail (then 500). **Implementation (12 files, no breaking changes)**: new domain VO `libs/domain/value_objects/character_video__valueobject.py` (CharacterViewSpec + CANONICAL_VIEWS); new infra exceptions ViewExtractFailed + AudioExtractFailed; new domain errors ViewExtractFailedError + AudioExtractFailedError; new `CharacterViewExtractor` class (~200 lines) appended to existing `libs/infrastructure/writers/character_video__writer.py` (reusing `CharacterVideoTruncator._is_under_character_folder` for sandbox validation); new `extract_views` method on `CharacterVideoCommand` (3rd dep `extractor` added to `__init__`); 4 new Cdtos (CharacterViewCdto / CharacterAudioCdto / CharacterViewFailureCdto / ExtractCharacterViewsResultCdto) + `views_to_cdto` mapper; new POST route mapping the 6 named domain errors to `detail.kind` strings; new Singleton `character_view_extractor` provider + updated `character_video_command` Factory in container; new `extractCharacterViews(path)` + 4 TS types in api.ts; new state `extractingViewsPath` + handler + 🖼 button in SiblingMedia.tsx (gated by `isCharacterVideoPath`). Boot smoke 7/7 pass. E2E smoke against `ai_videos/mozun_chongsheng/characters/c10_司空玄/c10_司空玄1.mp4` returned 200 with views=[front t=1.0s, side t=7.0s] + audio (the back at t=9.0s correctly failed because the source is a pre-v9 take shorter than 9s; partial-failure shape verified). **No spec-rule changes** — this is a downstream-of-v9 webapp tool, not a cross-cutting rule edit. See `follow_ups/093-20260518-203939-character-views-and-audio-extract.md`. **Prior bump:** 2026-05-18 19:49:56 — header bump for follow-up 092 (re-enable multi-angle character reference: **slow push-in + slow 360° orbit, single continuous take, 15s**, user-directed reversal of 091's v8 static-camera lockdown). v8 over-corrected by killing all motion: face never gets a close-up read (~1/6 frame at static全身远景距离), and 侧身/背面 silhouette reference is entirely lost — user rejects this trade-off. v9 hypothesis: Kling validator's "cut/transition" rejection was triggered by **speed + direction reversal**, not motion itself. v5/v6 ran 0.5s 360° whip-around (~720°/s spin blur + judged as cut) + v6 had 6 camera-move segments with push/pull reversals; v9 keeps motion but constrained to **≤ 45°/s orbit + monotone push-in + reverse-dolly hidden inside orbit arc + no direction reversals**. 5-phase continuous take: 0-2s static frontal wide (byte-identical to v8, preserves truncate contract) + 2-5s slow dolly-in to medium close-up (face clear at ~50mm framing) + 5-13s slow clockwise 360° orbit with concurrent slow reverse-dolly back to wide (reveals 正面 → 左侧身 → 背面 → 右侧身 → 回正面) + 13-15s lock back at wide for settle. dialogue table 5-row retimed (slot 3 = 2-5s, slot 4 = 5-10s, slot 5 = 10-15s); 0-2s 一/二 byte-identical preserved. Cross-cutting rule `.claude/agent_refs/project/ai_video.md` rule #12.5 v8 → v9: 时长 7s → 15s; 镜头 = "单镜头连续运镜 single continuous take · 5 阶段" replaces v8's "静态单镜头 locked camera · 零运动"; 动作 5 段 retimed for slow-motion beats; 节奏 = "缓慢连续运镜"; negatives swap (drop no-camera-motion / no-cut bans; add slow-motion-only ≤45°/s + no-reversal + no-stop-and-go + no-spin-blur 11-item ban list); v8 demoted to archive entry alongside v6/v5/v4. **Risk acknowledged**: v9 is a hypothesis about Kling's validator; if uploads still rejected, retreat to v9.1 (push-in only, 5s clip) or back to v8 7s static. Sibling mozun_chongsheng follow-up 025 (supersedes 024's v8 patch) ripples v9 to 10 character md files via one-shot script. **No webapp code change** — 2s trim path still slices byte-identical first 2s (0-2s segment unchanged from v8). User runs the patch script after reviewing this draft. See `follow_ups/092-20260518-194956-character-ref-slow-push-in-slow-orbit.md`. **Prior bump:** 2026-05-18 00:15:44 — header bump for follow-up 091 (rebuild character reference turntable around Kling's upload-validator constraint: **single shot, no cuts or transitions, clear character throughout**). Trigger: user reported Kling rejecting v6 15s renders with *"the current video contains cuts or transitions, and no clear, complete character is detected, please upload a single shot clear character video"*. Diagnosis: every prior version (v5 4s, v6 15s, planned v7 7s) structurally violates Kling — the fast 0-2s 360° orbit is flagged as "transition" and blurs the character so the detector misses; v6/v7 push-in/pull-out/pan in the tail compound the issue. v8 = **7s 全程静态正面单镜头 single take, 锁定机位, 零运动**. 5 timed beats all begin with "同机位同构图" as explicit anti-cut signal. 0-2s 一/二 byte-identical 跨角色 stays (truncate-compat preserved), but the 0-2s 360° silhouette pass is **dropped** — user explicitly approved the truncate-output downgrade since Kling acceptance is the hard prerequisite. 2-7s per-character dialogue from bibles. v7 (follow-up 090) was specced but never shipped; marked SUPERSEDED at top, kept on file as audit trail. Cross-cutting rule `.claude/agent_refs/project/ai_video.md` rule #12.5 v6 → v8 (skip v7); 镜头 6-move enumeration → static-camera declaration; 动作 7-segment → 5-segment with anti-cut prefixes; 8-row dialogue table → 5-row; negatives swap (drop 6-camera-move ban + 360° ban; add no-camera-motion / no-cut / no-turn-in-place bans); 时长 15s → 7s. Sibling mozun_chongsheng follow-up 024 (supersedes 023) ripples v8 to 10 character md files via `/tmp/patch_chars_v8.py` + 6-line cleanup of v5 360°-related stale negatives. 10/10 verified zero v5/v6 stale wording + character-specific dialogue intact in slots 4/5. **No webapp code change** — 2s trim path still works on v8 sources (slices frontal full-body + 一/二). Slot 090 = v7-superseded (kept for audit); this work occupies 091. See `follow_ups/091-20260518-001544-character-ref-7s-static-camera-kling-compat.md`. **Prior bump:** 2026-05-17 23:13:50 — header bump for follow-up 088 (bump character reference turntable from 4s to **15s casting reel**, giving each character proper time for casting: 6 camera angles + 3 character-specific signature lines from each role's `## 标志台词或口头禅` bible + silent expression range capture + 标志特征点 final-lock close-up. The 0-2s self-sufficient contract from 078 (一/二 + 正面定场 + 360° 回正, byte-identical across all characters) is **preserved** so ai_video_management's `_CONCAT_SEGMENT_S = 2.0` shot-char concat + `✂ 截到 2s` button continue to work). **No webapp code change** — 2s trim path already does what 088 needs (slices 0-2s); new 15s sources just have more material after the 2s mark. Cross-cutting rule `.claude/agent_refs/project/ai_video.md` rule #12.5 v5 → v6 (7-segment timed beats, 8-row dialogue table, 5 new v6 negatives, locked-fields per-character carve-out, design-principles rewrite, footer attribution). Sibling mozun_chongsheng follow-up 022 ripples to 10 character md files via one-shot Python script — each character gets its own 3 signature lines plugged into slots 4/5/7 (3-5s / 5-8s / 11-13s) + shortest line reused as slot 8 catch close (13-15s). 10/10 files verified zero stale v5 4s opener; c1 沧冥 + c10 司空玄 spot-checks confirm character-specific dialogue correctly wired. See `follow_ups/088-20260517-231350-character-ref-15s-proper-casting.md`. **Prior bump:** 2026-05-17 22:35:38 — header bump for follow-up 087 (fix the remaining half-body output that 085's canvas fix couldn't reach — this time the root cause was **prompt engineering anti-patterns**, not API geometry). Two structural fixes: (a) **negative-tokens-in-positive-prompt antipattern** — diffusion models parse each token's semantic meaning, not negation context, so writing `严禁 portrait` / `不要 头肩` / `生成失败 = portrait crop` inside positive prompt INJECTS the `portrait` token into the model's attention. Every 081→083 escalation made this worse. (b) **Kling's `negative_prompt` API field was unused** — all `严禁/不要/失败/portrait/half-body/close-up/crop` content moved out of positive prompt into Kling's dedicated `negative_prompt` field with separate inversion pass. Implementation: `KlingProvider.generate(...) + _submit(...)` accept `negative_prompt`; `ActorPool._build_prompts_for_slot(...)` now returns 3-tuple `(face, body, negative)`; all 4 generate-path sites (standard preview/generate + diverse preview/generate) plumb negative through. New `_NEGATIVE_PROMPT_ZH` constant (~390 chars EN+ZH comma-separated tokens) + `build_negatives()` helper. **Deleted** 3 framing-mandate constants `_LEADING_FRAMING_MANDATE / _RESTATE_FRAMING_MANDATE / _TAIL_FRAMING_MANDATE` (083 introduced them, all carrying the 严禁-language antipattern) + `_NEGATIVES_ZH` block. **New** single `_POSITIVE_COMPOSITION_TAG = "镜头：full body shot · head to toe · 9:16 vertical · long shot · 全身照"` — canonical diffusion-model composition keywords, positive-only. 4 builders rewritten with slim positive (descriptor + pool draws + wardrobe + 画面 + 摄影 + 要求). Positive prompt length **1660 → ~750 chars (-55%)**, well within Kling's effective attention budget. Smoke: 0 leaks of `严禁/不要` in positive ✓, negative_prompt threaded to Kling via `.generate(..., negative_prompt=neg_prompt)` ✓, preview payload carries `negative_prompt` for visibility ✓. pytest 15 pass / 3 pre-existing wukong fixture failures (= 085 baseline, 0 regressions). HTTP routes byte-identical (additive `negative_prompt` field in preview slot). Frontend unchanged. Slot 086 taken by parallel "actor-grid-assigned-filter" follow-up; this work occupies 087.

**Prior regen 34:** 2026-05-17 22:25:47 — header bump for follow-up 085 (fix the actual half-body root cause that 080-083 prompt-text escalation couldn't reach — the API request itself was contradicting the prompt). Three structural fixes: (a) face-shot `IMAGE_WIDTH/IMAGE_HEIGHT` 512×512 (1:1) → **720×1280 (9:16)** so Kling's canvas can physically hold a head-to-toe body; body shot bumped 576×1024 → 720×1280 too for consistency; `_kling_aspect_ratio(720,1280)` auto-maps to "9:16" at the API layer. (b) `定妆照` (Chinese beauty-headshot prior) replaced with `全身模特造型照 / fashion comp card full-body shot` across all 4 builder descriptor rows + `_CASTING_REQUIREMENTS_ZH`. (c) `_PHOTOGRAPHY_ZH` 10 entries rewritten — dropped 85mm 人像 / 哈苏中画幅人像 / Portra 400 / 105mm / SX-70 portrait priors, replaced with 24mm–50mm wide/standard lenses + explicit `全身` cue + landscape/fashion films (Ektar 100, Cinestill 50D). Also `_resize_jpeg` fixed from forced-square to longest-edge-preserving so 9:16 + "2k" → 1152×2048 instead of being squashed to a 2048-square. Smoke: canvas ratio 0.5625 = exact 9:16, 0/10 portrait-bias entries, all 4 builders carry new descriptor, resize math verified. pytest 15 pass / 3 pre-existing wukong fixture failures (= 083 baseline, 0 regressions). HTTP routes + JSON shapes byte-identical. Frontend (ActorGrid / ActorView / api.ts) unchanged. Slot 084 taken by parallel "frontend toast-TTL bug fix"; this work occupies 085. **Prior bump:** 2026-05-17 22:19:24 — header bump for follow-up 084 (frontend bug fix: delete-success toast 永不消失 — `lib/announce.ts` 的 shared `announceToast(msg, ttlMs=4500)` 自带 TTL 自动清除 + `.is-visible` 移除, 但 4 个 component 各自复制了一份缺 TTL clear 的本地 helper, 导致 `#aria-live-toast` region 一旦写入永久驻留。修复: Reader.tsx / ActorGrid.tsx / DeletedView.tsx 用 `import { announceToast } from "../lib/announce"` 替换本地 helper; SiblingMedia.tsx 用 aliased `import { announceToast as announce }` 保留本地 8 处 call sites 命名不动。补做 follow-up 060 utility 引入时漏掉的 caller migration). **Prior bump:** 2026-05-17 22:03:13 — header bump for follow-up 083 (escalate full-body framing to triple anchor: leading `【强制 MANDATORY · 全身从头到脚】` line + 10-waypoint anatomy restate line + tail `【强制构图 · 最后强调】` line, all 4 builder variants consume shared module constants `_LEADING_FRAMING_MANDATE / _RESTATE_FRAMING_MANDATE / _TAIL_FRAMING_MANDATE`. Opening descriptor row gains `**【强制全身】**` bold prefix as 4th anchor in high-attention prefix band. Triple-anchor smoke clean across all 4 variants. pytest 15 pass / 3 pre-existing wukong fixture failures (0 regressions). HTTP routes + JSON shapes + endpoint behaviors byte-identical.

**Prior regen 33:** 2026-05-17 20:38:12 — header bump for follow-up 082 (within-batch pool diversity for the 7 face/body pools — eyes / nose / lips / brow / contour / skin / body — so no two slots in a batch share the same descriptor unless the pool is genuinely exhausted). New `_batch_sample_pool` + `_resolve_batch_picks` helpers in `actor__chinese_prompt.py` implement bias-preferred + exhaust-then-fall-through + 25% wild-card. New `build_face_prompt_with_picks` / `build_body_prompt_with_picks` accept pre-resolved pool picks. `ActorPool._build_prompts_for_slot` dispatcher routes between batch-coordinated and legacy paths. `preview_prompts` + `generate_batch` + CDTO + Protocol + Pydantic body + TS API + ActorPoolGenerator.tsx all gain `batch_seed / batch_size / slot_index` optional fields; frontend computes `batchSeed = Date.now()` once per click and stashes the metadata on `previewBatchRef` so worker-pool generate matches preview byte-for-byte. Smoke: 10/10 distinct across all 7 pools vs legacy 4/10 eyes + 6/10 body for the same (count=10, look=sinister) setup. Bias-exhaustion (cunning, 2-element lips bias, count=10): lips still 10/10 distinct via fall-through. Determinism verified (same `batch_seed` → same picks). 081 framing line stays as the first line of every prompt. pytest 15 pass / 3 pre-existing wukong fixture failures (= 081 baseline, 0 regressions). HTTP routes + response shapes byte-identical. Scope-deferred: dropdown random_dim diversity + diverse-mode path.

**Prior regen 32:** 2026-05-17 20:26:25 — header bump for follow-up 081 (force every actor generation to render as a full-body head-to-toe photo by relocating the framing decision into the high-attention prefix). New leading `镜头：full-body wide shot / long shot, 9:16 竖屏构图, 头顶到脚趾完整入框, 头部上方 ~5% 顶边, 双脚下方 ~5% 底边, 严禁任何 portrait crop / head-shoulder framing / 半身像 / close-up。` line prepended to both prompt builders. Opening line rewritten to drop head-emphasis phrasing (`头部对焦` removed). 姿态 line `头部对焦清晰` → `头部清晰可辨` + appended `双手 + 双脚 完整入框不可被裁切`. 画面 line tightened to numeric framing (`头部占画面上 1/5 (留 4/5 给身体)`). `_NEGATIVES_ZH` escalated with four `**严禁**` imperative clauses (portrait crops / head > 1/4 / body < 70% / hand-foot edge bleed). Smoke (`build_face_prompt(east-asian/female/26-35, seed=42, femme_fatale)`): leading 镜头 line renders verbatim ✓, opening line carries new wide-shot phrasing ✓, 画面 line has numeric margins ✓, 4 严禁 clauses present ✓, module import clean. HTTP routes + JSON shapes + endpoint behaviors byte-identical (preview pane just renders the new prompt text). Historical generated jpgs untouched.

**Prior regen 31:** 2026-05-17 20:02:56 — header bump for follow-up 080 (actor casting comp-card wardrobe upgraded to industry **swimwear-standard** so every body metric the user named — 胖瘦 / 腿长短 / 腿型直弯 / 大腿内外侧线条 / 胸大胸小 / 胸型 / 肩宽 / 腰线 / 腰臀比 / 臀型 / 上身肌肉线条 — is readable from a single frame. `_casting_wardrobe('女性')` = "运动比基尼上装（窄肩带细带 + 紧贴胸型 + 完全露肩 / 露上胸 / 露背 / 露腹 / 露腰）+ 高叉紧身运动比基尼下装（高腰线露髋骨 + 高开叉露大腿全长 / 显腿型 / 腿长短 / 腿型直弯 / 大腿内外侧线条 / 臀型）+ 赤足"; `('男性')` = "上身赤膊（露胸肌 / 腹肌 / 肋骨线 / 肩宽 / 腰线 / 腰臀比）+ 紧身贴身运动短裤（高开叉短款露大腿全长 / 显腿型 / 腿长短 / 腿型直弯 / 大腿内外侧线条 / 臀型）+ 赤足". `_CASTING_REQUIREMENTS_ZH` lists 11 body metrics + 头顶到脚趾 + 一帧定格不裁切. `build_face_prompt` + `build_body_prompt` 画面 lines aligned 脚踝 → 脚趾. `_NEGATIVES_ZH` adds 3 guards: framing failures, modest-fallback wardrobe, glamour-pose drift. Pose unchanged. Smoke: `build_face_prompt(east-asian/female/26-35/modern-casual, seed=42, archetype=femme_fatale)` 服装行渲染新比基尼 verbatim + 画面行 toe-inclusive + 要求行 11-metric + 避免行新 3 段 ✓; module import clean. HTTP routes + JSON shapes + endpoint behaviors byte-identical (preview pane simply renders the new prompt text). Historical generated jpg files untouched; user re-renders as needed. Slot 079 collision with parallel "look-led-archetype-classification" follow-up; renumbered to 080.) 

**Prior regen 30:** 2026-05-17 11:39:48 — follow-up 079 (look-led `_classify_actor_attrs` fix: sinister-female → femme_fatale, was everyman; 4-priority router + cross-gender mapping for the 5 character-look slugs). 

**Prior regen 29:** 2026-05-17 19:28:26 — header bump for follow-up 078 (character reference turntable 2.9s → 4s + Chinese 「一, 二, 三」 + 「前 2s 自包含」 contract aligned with shot-character-reel `_CONCAT_SEGMENT_S = 2.0` trim and ✂ 截到 2s button. Cross-cutting rule `.claude/agent_refs/project/ai_video.md` §12.5 v4 → v5 patched: 4-segment timed beats (0-1 / 1-2 / 2-3 / 3-4), schema block, locked-fields list, design-tradeoff paragraph, attribution footer. No code change to `projects/ai_video_management/libs/infrastructure/writers/character_video__writer.py` — the auto-truncate-to-2s-before-concat behavior already exists from follow-up 054. Sibling follow-up `specs/ai_video/mozun_chongsheng/user_input/follow_ups/019-…-character-ref-4s.md` patches all 10 mozun_chongsheng character md files with the same byte-stable transformations. Slot 076 left empty: 077's changelog noted orphan "follow-up 076" comments in `actor__writer.py` about a future wardrobe/framing backfill — the slot stays reserved for that work to avoid topic conflation.)

**Prior regen 29:** 2026-05-17 19:22:20 — header bump for follow-up 077 (用户："在角色生成是，预览显示的prompt跟我所要的有比较大的出入，我在选项中已经选择了要淫邪的，但是预览里的10个prompt都跟淫邪不太相关，你可以在眼睛大小，鼻子形状等等细节自由发挥，但整体需要按我的要求来"). **新增** `_LOOK_FEATURE_BIAS_ZH` (5 个 character-look slug × 6 个 pool 的 index 子集 — sinister 偏 凌厉/狐眼/凤眼/挑眉冷峻/薄唇紧抿/长脸/不对称/魁梧 等；seductive 偏 桃花/狐眼/妩媚眼波/柳叶眉/丰唇致命诱惑/V字瓜子/丰满曲线 等；righteous / cunning / innocent 同理) + `_LOOK_OVERLAY_ZH` (5 条 ≥10 字中文气质句, sinister="阴邪冷峻, 似笑非笑, 隐含杀机, 城府难测, 阴险毒辣之气"). **`build_face_prompt` + `build_body_prompt`** 切换 5 个 五官+轮廓 行从 `_pick` → `_pick_biased(look_bias.get('eyes/nose/lips/brow/contour'))`；body bias 优先用 look bias 兜底 archetype；选 5 个 character-look 时在 `综合描述` 之后追加 `气质：{overlay}` 行；皮肤保留 uniform (074 决策 — 跨-archetype skin variety 最大化)。25% wild-card fallthrough 保留 — `0.75^6 ≈ 18%` 概率全 bias 命中，大多数 actor 至少 1-2 个 wild-card feature 保 within-batch 多样性 ("细节自由发挥")，但 整体气质 + 综合描述 + 气质 overlay 三层叠加保 整体一致。**Smoke** (sinister look, 30 seeds, archetype=None): 描述行匹配 sinister 关键词 30/30 vs handsome look 18/30；10 sinister seeds → 5 个 distinct 眼睛 descriptor (variety 保留)；handsome look prompt byte-identical to pre-077 (`_pick_biased` 退化 uniform，无 `气质：` 行)。HTTP routes + JSON shapes 不变。**Discovery (out of scope)**: `actor__writer.py` + `actor__chinese_prompt.py` 有 "follow-up 076" 引用但 follow-up 076 文件不存在 — 历史遗留，留待回填。Prior 075 switches all Kling actor-gen prompts from English variance-composed strings to **structured Chinese** (用户："所有生成的演员prompt请全用中文，并且符合一定格式，眼睛：{大小}, {形状}, ..., 鼻子：{大小}, {形状}, ..., 皮肤：{颜色}, {质地}..., 然后要有一段综合的描述，是妖艳的还是正值的，除了长相还要有形体的描述，高矮胖瘦"). **全中文结构化 actor prompt**: Kling 是快手中文模型，对中文 prompt 支持优于英文 (072 失败仅因 English 主体内 parens-CJK 切换破 tokenizer，纯中文无此问题)。**新文件** `libs/infrastructure/writers/actor__chinese_prompt.py` (per SRP — 一文件一关注点)：7 个中文 variance pools (每池 ≥ 20 条覆盖大小/形状/颜色多维度): `_EYES_ZH` (含 桃花/丹凤/鹿/狐/卧蚕/凤/杏) / `_NOSE_ZH` (含 高挺/驼峰/蒜头/挺直/小巧/塌/鹰钩) / `_LIPS_ZH` (樱桃/薄/厚/丰满嘟嘴/古典樱唇) / `_BROW_ZH` (剑/柳叶/远山/卧蚕/古典蛾眉) / `_CONTOUR_ZH` (方/V字/鹅蛋/圆/瓜子/国字/婴儿肥) / `_SKIN_ZH` (白皙→乌黑全色谱 + 玻璃/水光/哑光/婴儿肌/雀斑/沧桑 质地) / **新增 `_BODY_ZH` 体型池** (高挑修长/中等/娇小/魁梧 + 纤瘦/丰满/骨感/健硕/曲线/福态 — 用户要求 "形体的描述, 高矮胖瘦"); `_PHOTOGRAPHY_ZH` (10) 中文相机 cue; `_SYNTHESIS_BY_ARCHETYPE` 10 entries (`femme_fatale` → "一位妖媚妩媚的女配, 风情万种, 美艳动人, 致命诱惑" / `leading_hero` → "一位气场冷峻的男主, 英俊挺拔, 不怒自威, 王者风范" / etc.); `_BODY_BIAS_BY_ARCHETYPE` 10 entries (体型 bias per archetype + 25% wild-card fallthrough 保留 074); `build_face_prompt(attrs_dict, seed, archetype) -> str` 输出 13-line 结构化中文 (角色描述 / 眼睛 / 鼻子 / 嘴巴 / 眉毛 / 轮廓 / 皮肤 / 体型 / 综合描述 / 服装 / 摄影 / 要求 / 避免)；`build_body_prompt` 同结构 + 灰色 T 恤 + 黑色运动短裤 industry comp-card uniform 保留 (052) + 9:16 full-figure framing。**`actor__writer.py`** 改: `_build_face_prompt(attrs, variance)` → `_build_face_prompt(attrs, seed, archetype)` delegates ZH builder; `_build_body_prompt` 同; 4 call sites (preview/preview_diverse/generate_batch/generate_diverse_batch) 丢 `variance = _variance_for(...)` 直接 pass `seed + archetype` (or `spec.slug` for diverse 路径)。English `Variance` 机制现 dead code 对 wire 内容；留 source 不删 (deferred SRP cleanup)。`_CJK_PARENS_RE` strip 仍存活 (legacy guard) 但 no longer load-bearing。**Smoke (femme_fatale seed=42 示例)**: prompt 含 "眼睛：端庄秀眼, 双眼皮, 杏眼, 温柔贤淑" + "皮肤：深棕色, 巧克力质感, 性感浑厚" + "体型：高大魁梧, 健壮型男, 肌肉发达" + "综合描述：一位妖媚妩媚的女配, 风情万种, 美艳动人, 致命诱惑" + "避免：塑料感皮肤, 蜡像感, 卡通比例 ..." ✓; 18 pass / 5 pre-existing wukong fixture failures (=074 baseline, 0 regressions); main+asgi boot clean。Pure deterministic — same (seed, archetype) reproduces same draw; face + body share seed → 共享身份锚。HTTP routes + JSON shapes byte-identical (frontend preview pane 现显示中文 prompt — 069+070 modal/markdown wrap 已支持长 text)。**Prior 074/073/072/071/070/069/068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 28:** 2026-05-17 — header bump for follow-up 074 (用户："现在生成的actor差异性还是不够大，比如都是妩媚的，有些人可以嘴大，有些人可以嘴小，有些人和意识桃花眼，有些人可以皮肤白，有些人可以皮肤黑，所有五官，皮肤之类都要有20中不同的尺度，比如大小，颜色，形状"). **Within-archetype 演员差异性 step-change**: 三步走。**Step 1 — 扩 skin pools**: `_VARIANCE_SKIN_TONE` 10→22 (加 alabaster / translucent moonlight / cream rice / peachy rose-flushed / golden-honey / caramel / chestnut / umber / cocoa chocolate / ebony onyx / deep mahogany / neutral-medium — 用户提的 皮肤白/皮肤黑 全色谱); `_VARIANCE_SKIN_TEXTURE` 8→21 (freckled / glass-K-beauty / porous realistic / chok-chok dewy / scarred lived-in / ruddy windburn / matte velvet / silken satin / doll-flawless / sun-aged crinkled / pearlescent moonlit / olive-velvet matte / vellum parchment — tactile spectrum)。**Step 2 — 加 5 个中文眼型 到 EYES pool**: 22→27 (桃花眼 = peach-blossom upturned Tang allure / 杏眼 = apricot almond-tapered / 鹿眼 = fawn-like doe / 狐眼 = fox-shaped sly / 卧蚕 = silkworm-eyelid K-pop charm)；中文标 parens 内做 in-source docs，072 strip 保 wire clean。**Step 3 — Wild-card fallthrough in `_pick_biased`**: 新 `_BIAS_WILD_PROB: float = 0.25`；`_pick_biased` 改为 25% 概率 ignore archetype bias 走全 pool uniform，否则按 bias subset。6 个 biased 五官 → P(all archetype-biased) = 0.75^6 ≈ 18% → 82% 演员有至少一个 wild 五官 break sameness。Skin 不进 bias map (保 uniform random) → 跨 archetype skin variety 最大化。**Eye-shape bias 扩展**: 5 archetype 的 "eyes" subset 加新眼型 indices — `leading_warm` + 桃花/杏/鹿/卧蚕 (温润如玉)；`ingenue_kind` + 杏/鹿/卧蚕；`ingenue_lively` + 桃花/狐/卧蚕；`femme_fatale` + 桃花/杏/狐 (textbook 妩媚)；`youth_fresh` + 杏/鹿/卧蚕。其它 5 archetype 通过 wild-card 仍能命中 ~25%。**Smoke (30 femme_fatale gens)**: unique skin-tones 17/22 (alabaster→ebony 横扫) ✓; unique eye-shapes 8 (含 桃花眼 9× / 狐眼 8× / 杏眼 11×) ✓; CJK leaks 0/30 ✓; 18 pass / 5 pre-existing wukong fixture failures (=073 baseline, 0 regressions) ✓。**对比**: pre-074 同 30 gens 大约 ~4 unique skin tones + ~4 unique eyes — step-change 显著。Pure deterministic (same seed 复现); HTTP routes + JSON shapes byte-identical。**Prior 073/072/071/070/069/068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 27:** 2026-05-17 — header bump for follow-up 073 (用户："失败 2 张 — 查看原因 #2: actor_0096: write_failed: [Errno 2] No such file or directory: ... #4: actor_0099: ..."). **Concurrent race-condition bugfix in `_reap_incomplete_folders`**：前端 (per follow-up 064 unified mode + 059 worker-pool) 并行发 N 个 `count=1` 请求；每个请求进 `generate_batch` 顶部都 run reaper；reaper 旧版无 mtime guard — 删除任何无 jpg 的 actor 文件夹，包括 sibling 并发请求刚 allocate 但还在等 Kling HTTP (30-120s) 的文件夹。当 Kling 返回时该文件夹已不存在 → `write_bytes` 报 `[Errno 2] No such file or directory`。**修法**: 加 `_REAP_MIN_AGE_SECONDS: float = 300.0` 模块常量；`_reap_incomplete_folders` 每个 candidate 加 mtime guard `if entry.stat().st_mtime > cutoff: continue` (cutoff = now - 300s)。300s 安全覆盖 Kling worst-case (face 120s + body 120s + buffer)。OSError on stat 也 skip (defensive)。既有 keep-if-has-jpg 规则保留。**Smoke (临时目录 3 场景)**：fresh folder 不被 reap ✓ (修复 race); stale folder (mtime 回拨过 300s) 被 reap ✓ (genuine orphan cleanup 保留); 含 jpg folder 任 age 不被 reap ✓; 18 pass / 5 pre-existing wukong fixture failures (=072 baseline, 0 regressions)。**Class-of-bug**: 任何 "scan-then-act" 在并发场景都需 mtime/sentinel 防误删 sibling 工作；本 fix 用 mtime 简化方案，未来若想更激进 cleanup 可加 `.in_progress` sentinel file。HTTP routes + JSON shapes byte-identical; actor-folder schema 不变。**Prior 072/071/070/069/068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 26:** 2026-05-17 — header bump for follow-up 072 (用户："error when generate actor: 失败 6 张 — 查看原因 #1: 500 HTTP 500 ..."). **Bugfix to 071**: 6 slot 全失败的根因不是 Python 异常 — 是 Kling text-to-image API 在 wire-content 含 CJK-in-parens 注释时 silently 拒绝。Follow-up 071 把新 pool 条目 (如 `(高挺鼻梁)` / `(小眼睛)` / `(蒜头鼻)` 等 9 处) 作为 in-source docs 标在英文描述末尾，但条目直接 join 进 `features_text` 上传 Kling。修法：新 module-level `_CJK_PARENS_RE = re.compile(r"\s*\([^)]*[一-鿿][^)]*\)")` (CJK Unified Ideograph U+4E00–U+9FFF in parens) 在 `_variance_for` 末尾 `features_text = _CJK_PARENS_RE.sub("", ", ".join(parts))` 一次性 strip。**Source 注释保留** — 仍可在 actor__writer.py 读到 `"...nose... (高挺鼻梁)"` 知映射关系；只是上 wire 时变成 `"...nose..."` 纯 ASCII。**Smoke**: 6 seeds × 4 archetype-branches (含 None uniform) = 24 combos，`features_text` CJK 字符数全 0; 18 pass / 5 pre-existing wukong fixture failures (=071 baseline, 0 regressions); main+asgi import clean。**Class-of-bug**: 任何 bake 非-ASCII 进 LLM/text-to-image prompt 都要 sanitize；本 fix 选 source-保留 + assembly-strip 兼顾 readability 与 wire correctness。HTTP routes + JSON shapes byte-identical; 071 的 pool expansion + archetype bias 全部仍正常工作。**Prior 071/070/069/068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 25:** 2026-05-17 — header bump for follow-up 071 (用户："add more variety, 比如有些眼睛要大，有些是丹凤眼，有些是圆圆的眼睛，有些事小眼睛，有些是驼峰鼻，有些事高挺的鼻梁，有些事蒜头鼻，每一个五官都要有至少20中形容词，然后这些不同的五官一起钩织成了不同的演员。不同的五官选择的组合对应了不同类目的人"). **演员生成深度多样性 + archetype 五官连贯性**: 两步走改动到 `libs/infrastructure/writers/actor__writer.py`。**Step 1 — 面部 6 池扩到 ≥20 条**: eyes 14→22 (新增大眼睛/小眼睛/圆眼睛/泪眼 explicit Chinese-aesthetic variants + moonlit-silver/amber-honey/wide-set/close-set)；nose 10→21 (新增蒜头鼻/驼峰鼻/高挺鼻梁 Chinese descriptors + small petite/wide-nostril/hooked-raptor/snub/chiseled/ski-jump/K-beauty)；jawline 10→22 (peach/boxer/swan-neck/catlike/weak-chin/protruding/asymmetric/apple-cheek/K-beauty/ballet/lantern/fawn-curve)；cheekbones 9→20 (Asian-doll/painter-shadow/supermodel-hollow/Renaissance/diamond-cut/aristocratic/flat-plane/barely-there/asymmetric/peach-flush/marble-cool)；brow 10→21 (feline-flicked/maternal/pencil/gangster/asymmetric/pale-blonde/feathered/long-sweeping/blade-straight-K-beauty/caterpillar/puppy-dog)；lips 10→20 (tea-rose/pouty/crooked/Joker/heart-shape/Bardot/Anglo-Victorian/bee-stung/bashful/actor-trained)。原条目全保留 — 仅追加新条。**Step 2 — `_ARCHETYPE_FEATURE_BIAS` 10 archetype × 6 pool × ~5-8 indices each**: 例 `leading_hero` (英俊男主气场冷峻) → 方下颌+刀刻+古罗马+猫颚+K-beauty jaw + 高颧骨+painter-shadow+diamond-cut cheeks + 丹凤+深邃+锐利+暗黑+小眼 eyes + 鹰钩+古罗马+高挺鼻梁+chiseled+K-beauty nose；`femme_fatale` (妖艳女配) → V/heart/swan-neck/catlike jaw + Bardot/bee-stung/pouty/grin lips + 丹凤+heavy-lidded+catlike eyes；`ingenue_kind` (清纯善良女主) → 大眼+圆眼+wide-innocent eyes + 小巧/button/snub/ski-jump nose + tea-rose/cupid-bow/heart-shape lips。10 个 archetype 全有 bias entry。新 `_pick_biased(rng, pool, biased_indices)` helper：filter to subset → random.choice；biased 为空时回退 `rng.choice(pool)` uniform。`_variance_for(seed, gender, archetype=None)` 签名加 `archetype` 参数；4 处调用点 (`preview_prompts` + `preview_diverse_prompts` + `generate_batch` + `generate_diverse_batch`) 全部 forward `archetype` (standard/preview 路径接 kwarg；diverse 路径接 `spec.slug`)。`preview_prompts` 签名加 optional `archetype` kwarg (默认 None → 0 行为变化对旧 caller)。**Smoke**: 6 pool sizes 全 ≥20 ✓; 10 archetype bias keys ✓; uniform `_variance_for(42, 'male')` → 软椭圆下颌 + porcelain 颧骨; `leading_hero` biased → "powerful Roman-bust jawline" + "sharply angled cheekbones" (indices 7+3 命中); `femme_fatale` biased → "elongated swan-neck jawline" + "sharply angled cheekbones" (indices 12+3 命中) ✓; 18 pass / 5 pre-existing wukong fixture failures (= 070 baseline, 0 regressions)。**Out-of-scope (deferred — SRP/file-size flag)**: 把 variance pools 抽到独立 file (`libs/domain/value_objects/actor__variance.py` since they're business knowledge); `actor__writer.py` 现 ~2200 行，下次 stage-5 抓。非面部 pools (hair/skin/expression/lighting/mood) 保持 uniform — 用户只问五官。HTTP routes + JSON shapes byte-identical。**Prior 070/069/068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 24:** 2026-05-17 — header bump for follow-up 070 (用户："I can still see horizontal bar in frontend page when comes to prompt like in shotXX.md"). **Reader markdown `<pre>` overflow lock**: follow-up 069 仅修了 PromptPreviewModal 内的预览卡片，但 Reader 渲染 markdown 文件（shotXX.md / 角色 ref / shot pair）时，fenced ` ```text ` 代码块经 `.markdown-view pre` 渲染，默认 `overflow-x: auto` + `white-space: pre` → 长 prompt 出横条。三处 `<pre>` 容器同步改 wrap: `.markdown-view pre`（Reader markdown）+ `.code-view pre`（JSON/YAML）+ `.jsonl-line pre`（JSONL inline expand），都从 `overflow-x: auto` → `overflow-x: hidden` + `white-space: pre-wrap` + `overflow-wrap: anywhere` + `word-break: break-word`。`pre-wrap` 保留 `\n` 不破多行 prompt 结构；`overflow-wrap: anywhere` + `word-break: break-word` 让 comma-separated 1500-2000 字 prompt 内部 unbroken token 也能 break 到屏宽内。`.markdown-view pre` line-height 微调 1.6→1.65 让换行后字距更舒展。纯 CSS 改动 — `apps/ui/src/styles.css` 3 个 `<pre>` 块 rule 修改；零 JSX / 后端 / endpoint / spec FR 改动。Copy/paste 行为不变（wrap 仅视觉，剪贴板内容是 raw text）。**Prior 069/068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 25:** 2026-05-17 — header bump for follow-up 069 (用户："优化每个prompt在UI的展示，使得不过prompt多大,没有horizontal bar,并且让prompt看起来美观些"). **PromptPreviewModal 卡片优化 + 强制无横向滚动条**: 三层 overflow 防御保证 2000+ 字 prompt 不出横条 — (a) `.prompt-preview-card` outer `overflow: hidden`；(b) `.prompt-preview-body` `overflow-x: hidden + overflow-y: auto + max-height: 360px` 内滚；(c) `.prompt-preview-toggle / .prompt-preview-attrs / .prompt-preview-body` 三处都加 `overflow-wrap: anywhere + word-break: break-word` 让长 unbroken token 自由 break。同步美化：圆角 4→6 / padding 8/10→12/14 / `bg-toolbar`→`bg-panel` / hover `border-strong + 阴影` / `<ol decimal inside>` → `list-style: none`（与 meta 行 "第 N 张" 重复 marker 删）/ seed pill 圆角 10 + 1px border / attrs 加底色+边框+padding 5/9 / details 原生 marker 隐藏自定义 ▸/▾ 箭头 / body line-height 1.5→1.7 + font-size 12→12.5 / panel max-width 900→980 + width 90vw→92vw。纯 CSS — `apps/ui/src/styles.css` `.prompt-preview-*` 9 个 class 块重写；零 JSX / 后端 / endpoint / spec FR 改动。**Prior 068/067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 24:** 2026-05-17 — header bump for follow-up 068 (用户："lets hornor the single responsibility of the coding principle, in each py file, it should do only one thing and one thing well, if you see a file does two or more different things, it should be extract and move to another file, for example, the code below should not exist in a writer file: class InvalidPath / NotFound / FfmpegMissing"). **SRP applied to infra exception classes**: 43 个 `class Xxx(Exception)` 从 8 个 writer/reader 文件抽到 7 个新 `libs/infrastructure/errors/{aggregate}__error.py` (mirroring 现有 `libs/domain/errors/`)。Writer/reader 用 `from libs.infrastructure.errors.X__error import (...)` 重 import 维持 back-compat — commands 仍可 `from libs.infrastructure.writers.X import SomeException` 因为 writer re-export。**Common-level rule** 加 (agent_refs/project/development.md §1 + CLAUDE.md § Project rules): "Single Responsibility Principle — one concern per file"，列 4 个 concrete extractions: (1) exceptions → infra/errors/, (2) DAO dataclasses → infra/daos/, (3) DTOs → application/dtos/ (已 enforced), (4) Pydantic request bodies → route handler。**Extraction 实现**: 一次性 Python 脚本 `_extract_errors.py` (本 turn 后删除) 扫每个 writer/reader，regex 抓 `^class Xxx(Exception):` 块（含多行 bodies 如 `StaleWrite.__init__`），生成 per-aggregate errors files 带 proper docstring + 顶部 `from libs.infrastructure.errors.X__error import (...) # noqa: F401` re-import 重写 writer。Domain side `libs/domain/errors/{aggregate}__error.py` 不动 — 命名 distinction 保留：domain 是 `*Error` (app-layer 语义)，infra 是 bare names (filesystem/HTTP/subprocess raw failures)；commands catch infra → re-raise as domain。**Out of scope (deferred)**: (a) DAO dataclass extractions to `libs/infrastructure/daos/` (TruncateResult, MoveResult, RenameResult, GenerateResult, ActorInfo 等仍在 writer 文件 — 规则在位，下次 stage-5 review 抓)；(b) Commands 改 import path 直接从 errors 文件 (机械 cleanup，正交)。**Smoke**: 18 pass / 5 pre-existing wukong fixture failures (=066/067 baseline, 0 regressions)；`grep ^class \w+\(Exception\): libs/infrastructure/writers/*.py libs/infrastructure/readers/*.py` 0 matches；`find libs/infrastructure/errors -name "*.py" -not -name "__init__.py" | wc -l` = 7；main+asgi import clean。HTTP routes + JSON shapes byte-identical。**Prior 067/066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 23:** 2026-05-17 — header bump for follow-up 067 (用户："when I try to generate actors, I got 预览失败: 400 invalid_attribute"). **Domain/infra LOOK_OPTIONS sync fix**：DDD 拆层后 6 个 closed enums 在 domain `value_objects/actor__valueobject.py` + infra `writers/actor__writer.py` 各有一份；follow-up 064 加 5 新 look 值时仅碰 infra → application layer 的 `ActorQuery.preview_prompts` / `ActorCommand.generate` 调 `ActorAttrs.validate()`（domain）→ 旧 8 项 LOOK_OPTIONS 拒绝新值 → 路由层 400。修法：`libs/domain/value_objects/actor__valueobject.py::LOOK_OPTIONS` 加 5 新 slug + inline 注释 "MUST stay in sync with infra LOOK_OPTIONS"。**结构问题留单独 follow-up**：6 个 closed enums 都有 domain+infra 双定义；long-term 应让 infra `from libs.domain.value_objects... import LOOK_OPTIONS` — 单一 source of truth。**Smoke**：`ActorAttrs(look=righteous|sinister|seductive|cunning|innocent).validate()` 5 项全 pass；invalid look 仍正确 reject。零 frontend / spec FR / endpoint 改动。**Prior 066/065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 23:** 2026-05-17 — header bump for follow-up 066 (用户："make run-backend 报 ImportError: cannot import name 'create_app' from 'apps.api.routes'"). **Bugfix to follow-up 065**：`apps/api/main.py:13` + `apps/api/asgi.py:16` 还在 `from apps.api.routes import create_app` 旧位置 — 但自 follow-up 051 起 `create_app` 在 `apps/api/app_factory.py`，且 follow-up 065 把 `apps/api/routes` 拆成 per-aggregate **包**（`routes/__init__.py` 只 export 合并 `router`，不再 export `create_app`）。一行修：两文件 import 改为 `from apps.api.app_factory import create_app`。**Pytest 漏抓原因**：`tests/conftest.py:make_app` 在 065 中已修过，所以测试通过；`make run-backend`（走 main.py 入口）才触发 ImportError。**Smoke**: `python -c "import apps.api.main"` + `import apps.api.asgi` 均 0 errors；18 pass / 5 pre-existing wukong fixture failures（与 065 baseline 一致，0 regressions）。**Class-of-bug note**: 065 的 Python regex sweep 改 `from libs.infrastructure.X` 路径，但 `from apps.api.routes import create_app` 是 `apps.*` 不在 sweep 范围；conftest.py 后来手动修了，main.py + asgi.py 漏。未来 stage-5 可加 `python -m apps.api.main --no-reload` 入口 smoke（现 boot_smoke 走 `make_app()` 跳过 main/asgi 入口）。**Prior 065/064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 22:** 2026-05-17 — header bump for follow-up 065 (用户："add a rule, it is generally preferable to have a smaller file in terms of row count, preferrably less than 100, like routes.py file is to big, instead you should split that into multiple (name)__route.py folder and put then under a routes folder"). **Two changes**: (1) `agent_refs/project/development.md` §1 加文件大小 guideline (`< 100 行 preferable`, split by sub-concern when bigger; hard cap `~1000` 行 无清晰 sub-concern boundary = stage-5 `warning`; aggregates with legitimately complex business logic may exceed); `CLAUDE.md` § Project rules bullet 同步加。(2) `apps/api/routes.py` (847 行) 拆到 `apps/api/routes/{aggregate}__route.py` 8 个 per-aggregate 文件 (tree 18 / file 81 / media 204 / frame 54 / downloads 44 / actor 238 / casting 105 / character_video 84) + `_helpers.py` (55) + `__init__.py` (30, combined router). Each per-aggregate file owns its Pydantic bodies; aggregate-specific helpers stay with handlers; shared helpers (file_security_headers / method_not_allowed / actor_assigned_409 / map_move_failure) in `_helpers.py`。Container.py `wiring_config` 改 `packages=["apps.api.routes"]`（auto-wires all per-aggregate route modules）；`apps/api/{asgi,main}.py` + `tests/conftest.py` 同步 `container.wire(packages=...)`；`tests/test_*.py` `from apps.api.routes import create_app` → `from apps.api.app_factory import create_app`。**Pre-turn recovery**: 本 turn 开局发现 codebase 处于 OLD/NEW 混合状态 (5 flat infra 文件 + 11 OLD-path imports + container.py + agent_refs §1 + CLAUDE.md 都被回滚到 pre-051 状态，但 libs/{application,domain,infrastructure}/ 子目录 + app_factory.py + 本 turn 新写的 routes/ 都还在)。用户选择 forward-fix：移 5 flat infra 入子目录，删 5 superseded flat 文件，删旧 routes.py，rewrite container.py 到 post-061 状态，restore agent_refs §1 + CLAUDE.md project-rules bullets。**Note**: follow-ups 051/056/060/061 的更广 common-ref 工作 (§6b empty-application-layer blocker, §11b validation grep checks, §3 application-layer rewrite, §4 file-pattern table) 也被回滚但本 follow-up 未完整 re-restore — codebase 仍 enforce 规则，仅文档暂未引用。Future cleanup 可补。**HTTP routes + JSON shapes byte-identical**。**Smoke**: 18 pass / 5 pre-existing wukong fixture failures (=064 baseline, 0 regressions); App 构造 OK with 41 routes via 12 Q/C aggregate Factory providers。**Prior 064/063/062/061/060/059/058/057/056/055/054/053/052/051/050/049/048/047/046/045/044/043/042/041/040/039** 全部保持有效。

**Prior regen 21:** 2026-05-17 — header bump for follow-up 064 (用户："combine 标准模式和多样化随机模式 into one，in each dropdown, we could have an 随机option, when selectd, you can just randrom it, and 随机is the default value, also add more drop down option like 正义，阴邪，妩媚，狡诈，天真 etc"). **Unified generator mode**: 移除 standard/diverse mode radio；6 个属性下拉（民族/性别/年龄段/外貌气质/风格/画质）每个都加 🎲 随机 sentinel option 作 DEFAULT；用户可任意组合 — 部分字段固定 + 部分随机。**Frontend roll**: `apps/ui/src/api.ts` 加 `RANDOM_SENTINEL = "__random__"` + `rollRandomAttr<K>(field)` helper；`onPreview` 给每个 slot 生成 plan `{seed = Date.now()+i, attrs}`，遇 `__random__` 字段调 `rollRandomAttr` 滚一个具体值；并行 `Promise.all(slotPlans.map(plan => previewPrompts({count:1, ...plan.attrs, seeds:[plan.seed]})))` → 聚合成 `PromptPreviewResult`。每 entry 附 `attrs` 让 confirm 路径 forward per-slot resolved attrs。**Backend extension**: `actor__writer.py::preview_prompts` 加 optional `seeds: list[int] | None = None` 参数（previously 用 `time.time()` 毫秒精度推 base_seed，N 并行 count=1 调用会撞同 seed → 同 prompt → 同图，defeats randomness）；当显式 seeds provided，length validated == count，每 slot `seed = seeds[i]`；不 provided 走 base_seed + i fallback。`ActorRepository` Protocol + `ActorQuery.preview_prompts` 同步 plumb。**Look 枚举扩展**: `LOOK_OPTIONS` 加 5 个 slug `righteous / sinister / seductive / cunning / innocent`；新 `_LOOK_ENRICHED: dict[str, str]` 映射这 5 个到富 prompt fragment（e.g. "righteous" → "upright virtuous demeanor with steady honest gaze, dignified moral bearing"），`_build_face_prompt` + `_build_body_prompt` 用 `_LOOK_ENRICHED.get(attrs.look, attrs.look)` 取 — 旧 8 个 look 仍以 bare slug 入 prompt（Kling 接受 adjective），新 5 个走 enriched phrase。中文 label 走 `ATTR_LABELS_ZH.look`：正义 / 阴邪 / 妩媚 / 狡诈 / 天真。**Frontend label 汉化** (follow-up 063 sub-bundle): `ATTR_LABELS_ZH` 全 6 field 中文化（民族 / 性别 / 年龄段 / look + 5 new / style / resolution）；dropdown `<option>` 显示中文，`value=` 仍 slug。**Preview close on confirm fix** (follow-up 062 sub-bundle): `onConfirmGenerate` 启动 worker pool 前 `setPreview(null)` — 不再让预览模态遮住 ProgressPanel。**Modal × only close** (follow-up 058 carry-over): 移除 backdrop `onClick={onCloseRequest}` + footer "关闭" 按钮 — 仅顶角 × 可关闭 modal。**Count input 防卡 50** (follow-up 055+057 carry-over): `type="text" inputMode="numeric"` + `countText` string state + useMemo-style derived `count` + 三重 event guard (`stopPropagation` on click/mousedown/keydown + Enter preventDefault)。**Sandbox / endpoint shapes** unchanged — backend `LOOK_OPTIONS` 扩 closed enum 但 5 新值合规；既有 8 个 look slug 全部 backward-compatible；`preview_prompts` 新增 optional kwarg 不破调用方。**Smoke-tested**: new look enum + enriched prompts ✓；preview_prompts seeds 显式控制 ✓；frontend RANDOM_SENTINEL roll 路径 + per-slot Promise.all 并行 preview ✓。**Prior 045–063 全部保持有效**（含 058 modal × only / 055+057 count input fix / 052 face+body / 050 cast/ copy / 043 assignments / 040 _deleted bottom / 039 apps+libs DDD layout 等）。 (Backend boot failed with `RuntimeError: kling env keys missing; set KLING_ACCESS_KEY + KLING_SECRET_KEY`. Two reasons: [1] post-039 migration the project `.env` file was never recreated at the new canonical path `apps/api/.env` — follow-up 025's `backend/.env` is the same intent at the new location. [2] `apps/api/asgi.py` had a stale `Path(__file__).resolve().parent.parent / ".env"` left over from when it lived at `backend/libs/asgi.py` — at the new path that expression yields `apps/` (one level too high), disagreeing with `main.py`'s `parent / ".env"` = `apps/api/.env`. **Fix:** asgi.py now reads `parent / ".env"` (aligned with main.py); concrete Kling keys live in untracked `apps/api/.env` (gitignored line 138, verified by `git check-ignore`). **Spec walk:** any future text referring to `backend/.env` should read `apps/api/.env` going forward; no API / HTTP / behavior change. Prior 044 (lib/dramas.ts) + 043 (assignments) + 042/041/040/039 all remain in force.

**Prior regen 6:** 2026-05-13 23:05:00 — header bump for follow-up 044 (Vite `[plugin:vite:import-analysis] Failed to resolve import "../lib/dramas" from "src/components/ActorView.tsx"`. **Implementation gap from 043 item #9**: the `extractDramas` / `findChild` / `DramaChoice` extract from `ActorGrid.tsx` removed the inline definitions and updated both `ActorGrid.tsx` + `ActorView.tsx` to import from `../lib/dramas`, but the target file `apps/ui/src/lib/dramas.ts` itself was never written. **Fix**: write `apps/ui/src/lib/dramas.ts` containing exactly the logic captured from the last commit before 043 — `interface DramaChoice { path; name; characters[] }` + `extractDramas(tree)` walks `ai_videos` directory children (non-`_` prefix), pulls `characters/c\d+(_.*)?` subdirs into `characters` array + `findChild(node, name)` helper. **Zero behavior delta** vs. 043's intended state. Backend / HTTP routes / JSON shapes / `_cast.md` semantics / 409 refuse-if-assigned all unchanged. Prior 043 (assign-from-actor-page) remains in force.

**Prior regen 5:** 2026-05-13 22:47:37 — header bump for follow-up 043 (用户："lets add a new feature for the actor page, I could assign the actor to a specific role under a specific ai_video project. It is like multi dropdown. Once assigned the role, you need to maintain a link of the actor under the charactor folder, you could aslo see the picture of the actor. Note one actor could play many roles in differnet ai videos, but one ai vidoe charactor could only be one actor. When an actor has a role assigned, it cannot be deleted or archived."). **ActorView assignments + 角色 folder 链接 + delete 拒绝**：(1) 新 endpoint `GET /api/actors/assignments?actor_id=...` + `Casting.find_assignments_for_actor`; (2) `Casting.assign` / `unassign` 同步维护 `ai_videos/{drama}/characters/{role}/_cast.md`（actor face 图 + 链接），character folder 不存在则静默跳过；(3) `POST /api/actors/delete` 改为 **refuse-if-assigned**（不再 cascade-unassign），返 `409 actor_is_assigned`；(4) `POST /api/archive-media` & `POST /api/delete-media` 当 path 在 `_actors/actor_NNNN/` 下且 actor 有分配时同样 409；(5) `ActorView` 新增"🎬 角色分配 (N)"区块：列表 + 取消按钮 + 级联 drama→role dropdown + notes textarea + 确认；header `🗑 删除` 按钮在 assignments>0 时 disabled 并显 tooltip；(6) `App.tsx`/`Reader.tsx` 透传 `tree` 给 `ActorView`，`lib/dramas.ts` 共享给 `ActorGrid` + `ActorView`。**Spec walk**: FR-9g/FR-9h 描述 `_cast.md` 同步写/删；FR-9i 改写为 refuse-on-assigned；FR-9s 新增 `GET /api/actors/assignments`；FR-9c/FR-9k extension 块说明 archive/delete-media 在 `_actors/{id}/` 下的 409；FR-95 新 ActorView assignments section 契约。acceptance_criteria U3.23 全链路场景。**Prior 042 / 041 / 040 / 039** 全部保持有效；本 follow-up 不动 frame extraction / uvicorn / sidebar 置底 / DDD layout 任何路径。

**Prior regen 4:** 2026-05-13 22:50:19 — header bump for follow-up 042 (用户："I got some error again, the appliation just stucked: ...frames5.png HTTP/1.1 200 OK / WatchFiles detected changes in 'libs\\tree_walker.py'. Reloading... / Shutting down / Waiting for connections to close. (CTRL+C to force quit)"). **dev-reload 强制退出兜底**：follow-up 037 的 `timeout_graceful_shutdown=2` 让 uvicorn 在 2s 后 cancel asyncio task，但 FastAPI sync `def` endpoint 跑在 anyio threadpool 里，cancel asyncio wrapper 不会 kill 底层线程；Kling 30-120s HTTP / `/api/media` range stream / ffmpeg / pollinations 这些 blocking sync 调用让非-daemon 线程占住 Python 进程，导致 `sys.exit` join 线程时永久卡死。新 `libs/uvicorn_force_exit.py::install()` monkey-patch `uvicorn.Server.handle_exit`：signal 收到后除了走 uvicorn 自己的 graceful（仍是首选）外，再起一个 daemon `threading.Timer((timeout_graceful_shutdown or 0) + 2.0, lambda: os._exit(0))` 兜底；总时长 ≤ ~4s 进程必死。`main.py` 与 `libs/asgi.py` 顶部各调一次（reload 子进程经 asgi import，--no-reload 经 main）。**Spec walk**: FR-2 行追加 follow-up 042 amendment + 加 manual U2.5 acceptance + 覆盖矩阵 FR-2 行补 U2.5。041 / 040 / 039 narrative 保持不变；037 `timeout_graceful_shutdown=2` 是本 follow-up 的前置依赖（patch 读 `config.timeout_graceful_shutdown` 算 deadline）。

**Prior regen 3:** 2026-05-13 22:58:00 — header bump for follow-up 041 (用户："for the frames folder generated under a scene folder, I need better naming convention for the frame files, you should tell me things like if is hero wide or reverse wide, and there should be a fixed 8 pictures frames generated? ... please rank the pictures with order, in case I can only upload 3 as reference, I know which one to upload"). **Frame extraction v2**: 5 帧 → **8 帧**（5 个 canonical dwell + 3 个战略 transition：side / threequarter / mediumclose），命名约定从 `_f{N}_{role}.png` 改为 **`_r{rank}_{role}_{shot_size}.png`**（rank 1-8 = 上传优先级；字典序 = 优先级序），rank 1-3 覆盖三档不同焦段 medium / wide / telephoto 信息密度最高。**Renamer bug 同修**：`MediaRenamer.rename_drama` 在 `/api/rename-media` (api.py) + import-from-downloads 链式 rename (downloads_importer.py) 两条 caller 都新增/扩展 `excluded_folder_names` 排除 `"frames"`，避免之前的 `frames1.png ~ framesN.png` 命名丢失实测 bug。Extract 起点新增 `frames/*.png` sweep，自动 idempotent 清理 v1 残留 + 任何 rename 残留。`ExtractedFrame` API + TS interface 新增 `rank: int` 与 `shot_size: str` 字段。**Spec walk**: 新增 **FR-9r** 端点契约（8 帧 + rank + shot_size + sweep + rename 排除）。Prior 040 (`_deleted` 置底) 不变。

**Prior regen 2:** 2026-05-13 22:46:35 — header bump for follow-up 040 (用户："in ai_video_management left nav, lets move _deleted to the bottom of the left nav"). **Sidebar `_deleted/` 置底**：`tree_walker.py::_ai_videos_section` 分离 `_deleted` 节点，其它 ai_videos 顶层子目录（包括 `_actors/`）保持 alphabetical，循环结束把 `_deleted` append 到 children 末尾。纯 backend 改动，零 frontend，`/api/tree` shape 不变。**Spec walk**: FR-20 加 follow-up 040 amendment 行（`_deleted` hoist 规则）。Prior 039 `apps/+libs/` layout 改造仍未应用到代码；当迁移时该规则随 `tree_walker.py` 一起搬到 `libs/infrastructure/` 或 `libs/application/`，语义不变。

**Prior regen 1:** 2026-05-13 12:00:00 — header bump for follow-up 039 (项目采纳 `.claude/agent_refs/project/development.md` §1–6 的 `apps/+libs/` 解决方案布局：`backend/` → `apps/api/`，`frontend/` → `apps/ui/`，`backend/libs/*.py` 拆分到 `libs/{infrastructure,domain,application,common}/`，文件名/类名采用 `__` 后缀约定，加 `dependency_injector` 注入。HTTP 路由与 JSON 形状不变，只动内部组织。Prior 037 (uvicorn `timeout_graceful_shutdown=2`) 保持有效，调用位置改为 `apps/api/main.py`。)

**Prior follow-up 038:** 2026-05-13 22:23:41 — `_deleted/` 内 bulk hard-delete + 第 18 个 endpoint `POST /api/hard-delete-media` + `DeletedView` 多选页面 + typed-DELETE 模态 + sidebar `_deleted/` 行 "🧹 永久清理" 按钮（保持有效；039 把 `backend/libs/media_archiver.py` 移到 `apps/api/libs/infrastructure/...` 时需要保留 `hard_delete` 方法 + `NotInDeleted` exception + `_deleted/` 前缀校验；frontend `DeletedView.tsx` 移到 `apps/ui/src/components/`；HTTP route shape `POST /api/hard-delete-media` 不变）。

**Prior regen:** 2026-05-13 22:25:21 — header bump for follow-up 037（用户："once a while I will encounter errors and the system just stuck: WatchFiles detected changes... Shutting down... Waiting for connections to close. (CTRL+C to force quit)"）。**Dev reload 卡死修复**：`backend/main.py` 两条 `uvicorn.run(...)` 调用各加 `timeout_graceful_shutdown=2`（uvicorn ≥0.29 即支持，requirements 已固定）。根因：本项目 ~25 个 endpoint 全部同步 `def`，长任务（Kling face generation 30–120s / `/api/extract-frames` 1–5s / `/api/import-from-downloads` 5–20s）会持续占线程；uvicorn 默认 `timeout_graceful_shutdown=None` 让 reload 触发的 graceful shutdown wait-forever，user 看到 terminal 卡 `Waiting for connections to close` 行。加 2s 强制截断保证 dev 循环 reload 稳定。**Spec walk**: FR-2 行同步更新 `uvicorn.run` 调用 shape 描述。**Prior 036 (actor folder 折叠 + ActorView delete)** 保持有效。

**Prior follow-up 036:** 2026-05-13 22:23:53 — actor folder 折叠成单 leaf + ActorView 内置 delete（保持有效；037 不动 tree_walker / Sidebar / ActorView，仅修 uvicorn shutdown timing）。

**Prior follow-up 035:** 2026-05-13 11:00:00 — Extract Frames per-video button + `POST /api/extract-frames` 端点 + `frame_extractor.py`（保持有效；036 不动 SiblingMedia 与 frame extraction，仅折叠 actor folder 树节点）。

**Prior follow-up 034:** 2026-05-13 00:38:00 — actor md 读视图 + 无 bulk toolbar（保持有效；本 follow-up 035 不动 actor 视图，仅扩展 SiblingMedia 的按钮组）。
**Prior follow-up 033:** 2026-05-13 00:25:47 — actor jpg 文件名约定 `{ethnicity}__{gender}__{age_range}.jpg` + ActorGrid filter UI + idempotent migration（保持有效；034 不动文件名 / filter / migration，仅改 actor md 读视图）。

**Prior follow-up 032:** 2026-05-13 00:19:36 — Grid PAGE_SIZE 50 + preview-then-confirm prompt 流程（保持有效；033 不动 preview / seeds 流；preview 显示的 sidecar prompt 仍正确；034 新 ActorView 读视图复用同一 sidecar 的 prompt 字段，仅展示样式变化）。
**Prior follow-up 031:** 2026-05-13 00:16:00 — Anti-wax / no-AI-face prompt 改写 + 第 18 池 PHOTOREALISM camera cue（保持有效；033 不动 prompt 文本，仅改文件名约定 + 加 filter UI）。
**Prior follow-up 030:** 2026-05-13 00:11:16 — ActorGrid select mode + bulk delete + assign-character modal（保持有效；032 PAGE_SIZE 50 不影响 select 模式逻辑；选中跨页保留行为不变）。
**Prior follow-up 029:** 2026-05-13 00:00:12 — Rich variance (17 池 ≥1000 字符) + resolution selector (普通/2K/4K) + Pillow Lanczos resize（保持有效；032 预览的就是这些 17 池 + 031 加的第 18 池组装出的 prompt；seeds 流让 Kling 收到的与 preview 完全一致）。
**Prior follow-up 028:** 2026-05-12 23:43:09 — ActorGrid 视图 + 分页（保持有效；030 在其上加 select mode + bulk delete + assign 模态；分页 + 单 tile click → detail 行为不变）。
**Prior follow-up 027:** 2026-05-12 23:26:56 — 9-way 并发 + per-image variance + race-safe ID 分配 + cap 50（保持有效；029 在其 variance pool 基础上扩展到 17 池 ≥1000 字符，race-safe + 并发不动）。
**Prior follow-up 026:** 2026-05-12 23:10:14 — actor folder delete + cascade unassign（保持有效；与 028 正交 — 026 改写 delete 路径，028 加新 read view）。
**Prior follow-up 025:** 2026-05-12 22:51:47 — Kling-only provider + `backend/.env` + `libs/env_loader.py` failfast（保持有效；027 仍用同一 KlingProvider，仅前端并发数 + prompt variance + race-safe 分配改动）。
**Prior follow-up 024:** 2026-05-12 23:30:00 — Kling text-to-image 作为第 3 个 provider 加入 chain（已被 025 部分覆盖：KlingProvider + JWT + aspect ratio mapper + SSRF-vet 保留并升为唯一 provider；chain 抽象本身被 025 删除）。
**Prior follow-up 023:** 2026-05-12 22:15:39 — mp4 / image reader Delete 按钮 + `_deleted/` 目录 + `POST /api/delete-media`（保持有效；与 025 正交）。
**Prior follow-up 022:** 2026-05-12 22:07:24 — sidebar 顶部 collapse-all 图标按钮（保持有效；与 023/024 正交）。
**Prior follow-up 021:** 2026-05-12 23:00:00 — multi-provider face generation 架构（保持有效；backend `actor_pool.py` provider chain，与 023 不相干 surface）。
**Prior follow-up 020:** 2026-05-12 21:57:51 — 收窄 follow-up 019：mp4/image reader 单按钮 archive（保持有效；follow-up 023 在 archive button 旁加 delete button + 在 `_deleted/` 内隐藏两按钮 — archive 可见性规则的小幅扩展）。
**Prior follow-up 019:** 2026-05-12 21:43:45 — Reader 渲染 dispatch 在 video / image / ImageRefView / ShotPairView 分支挂 SiblingMedia 修归档 UI 不可见（保持有效但 follow-up 020 把 video + image 分支收窄到单按钮；imageRef + shotPair 分支仍走 SiblingMedia）。
**Prior follow-up 018:** 2026-05-12 22:30:00 — pollinations.ai 429 rate-limit cascade 修复 + incomplete actor folder reclaim（保持有效；与本 follow-up 不相干 surface）。
**Prior follow-up 017:** 2026-05-12 22:00:00 — frontend loop count=1 + progress bar 修 batch generate UX（保持有效；follow-up 018 在此 progress UX 基础上加 throttle phase + backend retry）。
**Prior follow-up 016:** 2026-05-12 21:30:00 — jpg/png preview 走 /api/media，修 base64 fall-through（保持有效）。
**Prior follow-up 015:** 2026-05-12 21:05:00 — eager-mkdir `_actors/` on startup 修 follow-up 014 的 chicken-and-egg bootstrap bug（保持有效）。
**Prior follow-up 014:** 2026-05-12 20:15:00 — actor face pool + casting workflow（保持有效；follow-up 015 + 016 + 017 + 018 修其 UX / 稳定性 bug，行为契约不变）。
**Prior follow-up 013:** 2026-05-11 12:50:29 — 一次性 data-op 把 19 个 character mp4 trim 到 ≤ 2.9s（保持有效；ref-video 生成仍遵守此约束）。
**Prior follow-up 012:** 2026-05-11 12:28:33 — backend `--reload` default（保持有效）。
**Prior follow-up 011:** 2026-05-11 20:25:46 — SiblingMedia multi-select + 批量 archive/unarchive（保持有效）。
**Prior follow-up 010:** 2026-05-11 12:04:54 — scene reference video 3.9s all-angle 序列（cross-project rule，webapp 不受影响，保持有效）。
**Prior follow-up 009:** 2026-05-11 19:56:38 — 导入 + 重命名一键流程（保持有效）。
**Prior follow-up 008:** 2026-05-10 20:18:26 — per-file archive / unarchive media（保持有效；011 在其基础上加批量层）。
**Prior follow-up 007:** 2026-05-10 17:04:38 — drama-level rename-media 按钮 + `POST /api/rename-media`（保持有效）。

**Prior follow-up 006:** 2026-05-10 16:40:54 — runtime reload procedure documentation（保持有效 — 仅 documentation）。

**Prior follow-up 005:** 2026-05-10 16:18:39 — media display + playback (backend `/api/media` route + frontend Reader video/image rendering + SiblingMedia gallery)（保持有效）。

**Prior follow-up 004:** 2026-05-09 19:48:37 — 中文 filename opt-in（保持有效）。
**Prior follow-up 003:** 2026-05-09 15:21:35 — research/ folder + viewer（保持有效）。

## Goal

Build a focused viewer / editor SPA + FastAPI backend at `projects/ai_video_management/` for the artifacts under `ai_videos/{name}/` — character bibles, Seedream立绘 prompts, style guides, scripts, shotlists, dual Kling+Seedance shot prompts, publish metadata, README. The webapp ships three custom view modes that make the ai_video output structure navigable: **ShotPairView** (Kling + Seedance side-by-side), **ShotlistTableView** (clickable shot-row navigation), and **ImageRefView** (Seedream prompt + companion `.png` preview). It reuses spec_driven's security posture (Origin/Host gate, EXPOSED_TREE sandbox, RFC 7232 mtime concurrency, IPv4 loopback, light theme, CSP) but targets a single tree root: `ai_videos/`. Bound port: **8766** (parallels spec_driven's 8765 for simultaneous run).

**Per follow-up 001 + 002:** the webapp does not load, read, reference, or anchor on `specs/`, the workspace `CLAUDE.md`, or `.claude/` — at any layer. Source files contain zero references to those paths. Regen-prompt and pinning features are out of scope; users who need to drive an ai_video pipeline run regen prompts through a separate webapp dedicated to that purpose. `RepoRoot.find()` anchors on `ai_videos/` directory presence, the only path this webapp cares about.

## Context

- **Why a focused tool:** the existing `spec_driven` webapp already manages the spec pipeline (intake, interview, findings, final_specs, validation, regen prompts, pinning) for both `development` and `ai_video` task types. The user wants a complementary tool that opens directly into the *output* tree of ai_video projects, without forcing them through the spec-pipeline navigation chrome. Division of concern: spec_driven for the pipeline; ai_video_management for the artifacts.
- **Why a parallel of spec_driven structurally:** the security posture (Origin/Host gate, sandbox, traversal hardening, mtime concurrency, IPv4 loopback only, light-theme chrome, extension allowlist, CSP) is battle-tested. Reusing it verbatim is the right call. The render-mode dispatch + sidebar pattern also ports — it's just the EXPOSED_TREE membership and the stage-pipeline features that change.
- **What's specific to ai_video output:** the `prompts/shot{NN}_{kling|seedance}.md` paired-file pattern, the `shotlist.md` table that needs row-click navigation, the `characters/ref_images/{role}_seedream.md` + companion `.png` preview, and the locked-descriptor block sentinel that should render with a "锁定块" pill. These four cases drive the three new view modes plus the locked-block pill in MarkdownView.

## Desired outcome

A `projects/ai_video_management/` folder following the same structural conventions as `projects/spec_driven/`, but functionally narrowed to ai_videos/:

```
projects/ai_video_management/
├── README.md
├── Makefile                       # install / install-* / run-prod / run-backend / run-frontend / run / test-* / e2e / boot-smoke / clean
├── backend/
│   ├── main.py                    # ~15 lines — argparse + create_app + uvicorn
│   ├── requirements.txt
│   ├── libs/
│   │   ├── repo_root.py           # find CLAUDE.md + .claude/ to anchor (NOT exposed)
│   │   ├── safe_resolve.py        # path traversal hardening; allowed top-level = {"ai_videos"}
│   │   ├── exposed_tree.py        # is_inside admits ONLY ai_videos/**
│   │   ├── file_reader.py         # GET /api/file: text + image (.png/.jpg) routes
│   │   ├── file_writer.py         # PUT /api/file: text only, If-Unmodified-Since required
│   │   ├── tree_walker.py         # single-section "AI Videos" tree
│   │   ├── sub_type_lookup.py     # heuristic: episodes/ exists → novel, else → short
│   │   ├── api_security.py        # Origin/Host gate + CSP middleware
│   │   ├── api.py                 # 2 endpoints only: GET /api/tree, GET/PUT /api/file
│   │   └── __init__.py
│   ├── static/                    # bundled SPA for single-process mode
│   └── tests/                     # pytest unit + boot-smoke + Origin/Host shapes
└── frontend/
    ├── package.json               # react-resizable-panels for ShotPairView/ImageRefView
    ├── vite.config.ts             # Vite proxy /api/* → 127.0.0.1:8766 with Origin rewrite
    ├── tsconfig.json
    ├── index.html
    ├── src/
    │   ├── main.tsx
    │   ├── App.tsx                # Routes: / and /file/* only (no /project, no /stage)
    │   ├── api.ts                 # 3 calls: fetchTree, fetchFile, putFile (+ imageUrl helper)
    │   ├── localStorage.ts
    │   ├── types.ts               # TreeNode + ProjectMeta + FileResult — no Stage/Regen/Promote types
    │   ├── styles.css             # light-theme chrome + dark <pre> carve-outs
    │   ├── components/
    │   │   ├── Sidebar.tsx        # single section "AI Videos" with sub-type badges
    │   │   ├── Reader.tsx         # render-mode dispatch (no regen-panel, no cross-tree link)
    │   │   ├── Editor.tsx
    │   │   ├── BrokenLink.tsx
    │   │   ├── Breadcrumb.tsx
    │   │   ├── Home.tsx
    │   │   ├── ParseFallback.tsx
    │   │   ├── ShotPairView.tsx
    │   │   ├── ShotlistTableView.tsx
    │   │   └── ImageRefView.tsx
    │   ├── markdown/
    │   │   ├── renderer.tsx       # locked-block "锁定块" pill via pre-render regex
    │   │   ├── CodeView.tsx
    │   │   ├── JsonlView.tsx
    │   │   └── ImagePlaceholder.tsx
    │   └── lib/
    │       ├── linkResolver.ts
    │       ├── shotPairing.ts
    │       └── shotlistParser.ts
    ├── e2e/                       # Playwright (2 mode profiles)
    └── test/                      # Vitest
```

**Removed compared to v0** (per follow-up 001):
- Backend: `regen_prompt.py`, `promotions.py`, `stages.py`. `/api/regen-prompt`, `POST /api/promote`, `DELETE /api/promote`, `/api/stages` endpoints all dropped.
- Frontend: `RegeneratePanel.tsx`, `ProjectPage.tsx`, `StagePage.tsx`, `QaView.tsx`, `QaErrorBoundary.tsx`, `lib/autonomousMode.ts`, `lib/qaParser.ts`. Routes `/project/...` and `/stage/...` dropped.
- TreeNode sections: "Specs" and "Context" sections gone — sidebar emits ONLY the "AI Videos" section.

## Hard constraints (preserved from v0)

1. Localhost-only, IPv4 (`127.0.0.1`); IPv6 + `0.0.0.0` explicitly out of scope.
2. Origin/Host gate on every state-changing endpoint (now just `PUT /api/file`); foreign / wrong-port → 403; loopback aliases admit.
3. Path traversal hardening: every probe collapses to a single 404 (no existence oracle); symlinks/junctions refused.
4. Extension allowlist for reads: `.md`, `.json`, `.yaml`, `.yml`, `.jsonl`, `.txt`, `.png`, `.jpg`. SVG NOT allowed. Image extensions NOT writeable (PUT rejects with 400).
5. RFC 7232 `If-Unmodified-Since` required on PUT for existing files (divergence from spec_driven retained); stale → 409, missing → 400.
6. Markdown sanitization: `rehype-sanitize` default schema.
7. CSP header on responses: `default-src 'self'; img-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self'; object-src 'none'; base-uri 'self'`.
8. Light-theme chrome per `agent_refs/project/development.md` rule 1; dark `<pre>` carve-outs only.
9. FastAPI + Python 3.11+ + strong-typed `@dataclass(frozen=True)` containers; pip-only.
10. React + TypeScript + Vite + react-resizable-panels.
11. Bound port 8766 (Vite dev 5174); coexistence with spec_driven (8765/5173) intact.
12. EXPOSED_TREE membership: **single root — `ai_videos/**/*.{md,json,jsonl,yaml,yml,txt,png,jpg}`**.
13. localStorage namespace `ai_video_management.*` (still relevant for any future cross-tab state).

## Out of scope (v1, post-follow-up)

- Spec-pipeline navigation: `specs/`, `CLAUDE.md`, `.claude/` are not in EXPOSED_TREE.
- Regen-prompt assembly: not a feature of this webapp. Use `spec_driven` for that.
- Pinning (`<stage>/promoted.md`): not a feature.
- QaView render mode for spec interview pages: not applicable.
- Cross-tree link "查看规格": removed.
- Render-API integration with Kling / Seedance / Seedream (text-prompt viewing/editing only).
- Multi-tenant / multi-user; auth; WebSockets; mobile-responsive; storyboard horizontal-scroll view; cross-publish manager; English translation; project-diff view; file create/delete/upload through the webapp; dark-mode chrome toggle; file-system watcher.

## Success looks like

1. `make run-prod` → open `http://127.0.0.1:8766/` → recursive sidebar shows ONE section: AI Videos. The `wukong_juexing` entry has a `短` badge.
2. Click `ai_videos/wukong_juexing/prompts/shot02_kling.md` → ShotPairView with Kling on left + Seedance on right + per-pane copy buttons.
3. Click `ai_videos/wukong_juexing/shotlist.md` → ShotlistTableView; each `shotNN` cell is a button → opens that shot's pair view.
4. Click `ai_videos/wukong_juexing/characters/ref_images/main_seedream.md` → ImageRefView; left = prompt, right = companion `.png` if present, else fallback message.
5. Click `ai_videos/wukong_juexing/characters/main.md` → MarkdownView with the **锁定块** pill on the descriptor.
6. No "Specs" section. No "Context" section. No regen-prompt panel. No cross-tree link.
7. PUT `/api/file` succeeds for editable text files; rejects images with 400; missing `If-Unmodified-Since` returns 400; stale returns 409.
8. spec_driven on 8765 continues to handle regen + pinning for ai_video projects (unchanged).
