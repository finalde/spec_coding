# Follow-up draft 035 — 2026-06-14

shot prompt 不提字幕，只放台词 + 标 正常台词/内心独白（内心独白嘴不动）。

---

Common-level 规则（已写入 `.claude/agent_refs/project/ai_video.md` §12.4 v2 + CLAUDE.md）：shot 视频 prompt 的台词字段不得出现任何字幕信息，字段 label `台词 / 字幕:` → `台词:`，每条台词标 `正常台词` / `内心独白`（内心独白嘴唇不动）。

nvdi 本就遵循「台词字段不含字幕排版」（nvdi 028）且已带在画人物口型契约——本次迁移只做：① label `台词 / 字幕:`→`台词:`（28 文件）；② 内心独白 OS 行的「为画外配音 / 字幕」去掉「/ 字幕」。台词原文与既有口型契约 verbatim 保留；nvdi 既有的 `画外音/朗声 + 在画人物口型` 标注已表达正常/内心独白之分，未强行改写为二元标签。
