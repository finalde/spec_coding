---
target_stage: 6
target_artifacts:
  - .claude/agent_refs/project/ai_video.md
  - episodes/ep01/shots/*/shot*.md
severity: medium
---

# Follow-up draft 007 — 2026-05-31
非说话镜需给 Kling 显式闭口指令 —— shot04 (默剧) 生成时角色乱动嘴/鸟语。

## 抽象指令

shot04 是无台词静默 reaction，但 Kling 生成的视频里角色还是动嘴说了些听不清的鸟语。根因：Kling / Seedance 等模型默认给在画人脸自动叠加说话口型，弱表述（如「(静默, 无台词)」）不足以抑制。需给模型更明确的指令。

决议（用户选「全部非说话镜」13 镜）：凡在画人物在该镜**不出声**的情形，`台词 / 字幕:` 行显式追加 `· 在画人物口型: {角色}全程闭口、嘴唇不动、无说话口型`，且 `负向:` 行加 `不要 说话 / 不要 嘴部开合 / 不要 说话口型 / 不要 lip sync / 不要 自动配音` 等反向词。三类：
- 纯静默 / 仅环境音：shot04, 08, 11, 14, 16, 18, 24
- V.O. 内心独白（角色在画但声音为画外配音）：shot06, 15, 17, 27, 28（须注明 OS 非现场出声、严禁对口型）
- 听旨方（台词系他人 OS）：shot02（父子听太监 OS）

并作为通用规则写入 ai_video.md（rule 5 v3 台词三选一契约下新增「在画人物口型契约」）。
