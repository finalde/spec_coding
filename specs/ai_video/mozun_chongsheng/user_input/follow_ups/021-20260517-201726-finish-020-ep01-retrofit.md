# Follow-up draft 021 — 2026-05-17

User re-asks 15s shot rule —— follow-up 020 already declared 全局 15s + 50 shot mds 落地，但 020 同 turn 只完成了 ep02-ep05 (40 shots) + ep01/shot01-03 (3 shots) + 4/5 shotlists；ep01/shot04-shot10 (7 shots) + ep01/shotlist.md 仍停留在 legacy 10s schema。本 021 = 020 的 ep01 retrofit 收尾，不引入新规则。

## 用户原话

> 在ai vidoes里，所有的shot都改为15s，这应该会给你更多的空间描述镜头，动作，和台词

（语义与 020 的 "change all the shots to be in 15s length, this should give you more room for more details for each shot, both 台词，运镜and 动作" 完全一致；021 是补完，不是新规则。）

## 决策

| 项 | 选 |
|---|---|
| 是否新增规则 | **否** — 全局 15s 规则与 12.4-B `时长: 15s` 表述在 020 同 turn 已落地 (`CLAUDE.md § AI video rules` bullet 1 + `agent_refs/project/ai_video.md` 规则 #6 / 12.4 表 / 12.4-B 行) |
| 是否新增 schema 字段 | **否** — 台词 first-class shot field 在 020 已宣告（CLAUDE.md "Visuals only in v1" 收窄为 audio synthesis only） |
| 是否新增 acceptance trigger | **否** — 沿用 020 已定义的 8 条 acceptance 条款 |
| 落地范围 | ep01/shot04-shot10 (7 shots) + ep01/shotlist.md (1 file) — 020 同 turn 漏掉的 ep01 retrofit；ep02-ep05 不动 (020 已完成) |
| 是否 walk 上游 artifacts | **否** — 020 已经在 revised_prompt.md / spec.md / changelog.md 留下记录；021 只是 020 的 ep01 retrofit 收尾，不改变 spec |

## 改动范围

### 项目代码 / shot mds（本 follow-up 同 turn 落地，7 shot 并行 agent）

1. 重写 `ai_videos/mozun_chongsheng/episodes/ep01/prompts/shot{04..10}/shot{NN}.md`（7 个文件）：
   - Shot context 块 `Duration: 10 seconds — schema 12.4-B 默认（Kling 10s 单次生成上限）` → `Duration: 15 seconds — per agent_refs/project/ai_video.md rule #6 (15s default/target; Kling 渲染 split 为 2 个 back-to-back ≤10s + ≤5s call，中间用 \`shotNN_lastframe.png\` 做 mid-seam). 由 follow-up 020 由 10s 扩到 15s。`
   - Timed beats 重写为 5–7 拍覆盖 0–15s（每拍 2–3 s）。
   - `镜头:` 行从 5–6 个 time window 扩到 5–7 个（多 1–2 个 cut 在 10–15 s 段）。
   - `运镜:` 行同步扩 5–7 个 transition (entry 数与 `镜头:` window 数一致)。
   - `动作:` block 每拍重写覆盖 0–15s；10–15 s 新拍必须 advance plot / reveal / reaction（不能是 idle 站立）。
   - `台词 / 字幕:` block 至少保留 1 行 dialogue（已有 dialogue 的 shot 在 10–15 s 段加 1 行 reaction-or-reveal 台词；silent shots 保留 `无台词 / 默剧` 但必须 explicit 注释）。
   - `时长: 10s` → `时长: 15s`。
   - **不动**：Reference placeholders 表 / `场景:` line / `光线/色调:` / `节奏:` (允许 tail 调到覆盖 0-15s，preserve 节奏 word) / `渲染样式:` / `比例:` / `负向:` / Seam-frame still prompts 段。
   - **2000-字 soft cap** 保留（prompt body ≤ 2000 字）。

### 项目代码 / shotlist（同 turn 落地）

2. 重写 `ai_videos/mozun_chongsheng/episodes/ep01/shotlist.md`：
   - 表头 `总时长：90 秒 ｜ 10 镜 ｜ 9:16 竖屏` → `总时长：150 秒（10 镜 × 15s — per agent_refs/project/ai_video.md rule #6 / follow-up 020 由 90s 扩到 150s）｜ 9:16 竖屏`。
   - 时长列：8/8/9/9/9/10/10/9/9/9 → 全部 15s。
   - 三钩落点 anchor 重算：黄金钩 0-15s (shot01) / 第一反转 45-60s (shot04) / Cliffhanger 120-150s (shot09 + shot10)。
   - 时长合计 `90 秒` → `150 秒`。
   - 内容描述列保留 shot 主体 narrative beat。

### Pipeline 状态（本 follow-up 同 turn 不动）

- `revised_prompt.md` `Last regenerated` 段 header bump 为 020 (已存在) + 021 (本次 retrofit 收尾) — 021 不引入新规则故不需要 full regen，只在 changelog 留尾记。
- `interview/qa.md` / `findings/` / `final_specs/spec.md` / `validation/` 不动 — 020 已在 spec.md 顶部追加 amendment block, 本 follow-up 不改变 spec。
- `changelog.md` append 021 entry。

## 不在本 follow-up 范围

- ep02-ep05 的 40 个 shot + 4 个 shotlist - 020 已扩到 15s, 本次不再 touch。
- 全局 rules (CLAUDE.md / agent_refs/project/ai_video.md / agent_refs/validation/ai_video.md) — 020 已落地。
- character bibles / scenes / style_guide / arc_outline / episode.md / publish.md — 不动。
- 已生成的 mp4 / png 输出 — 不动 (用户后续 stage-6 manual render 一次)。
- Seam-frame `shotNN_lastframe_seedream.md` 内容 — 主体 frozen instant 定义不变。

## Acceptance trigger（沿用 020）

- ep01/shot04-shot10 (7 文件) `时长: 15s` 且 Shot context `Duration: 15 seconds`。
- 7 个 shot 的 `动作:` block beats 覆盖到 0–15s（最后一拍 end-time = 15）。
- 7 个 shot 的 `镜头:` 行 ≥ 5 个 time window，至少 1 个 window 落在 10–15s 段。
- 7 个 shot 的 `运镜:` 行 entry 数与 `镜头:` window 数一致。
- 7 个 shot 的 `台词 / 字幕:` block 至少 1 行 dialogue OR 显式 `无台词 / 默剧` 注释。
- 7 个 shot prompt body 字数 ≤ 2000（hard cap 2500）。
- ep01/shotlist.md 时长列由 8-10s → 15s，总时长 → 150s。
