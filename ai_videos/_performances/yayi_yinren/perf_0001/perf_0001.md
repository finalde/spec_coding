---
perf_id: perf_0001
emotion: 压抑隐忍
intensity: 1
style: 内敛压抑
carrier: 眼神
au_ref: [AU1, AU4, AU43]
lma_tag: bound-flow / sustained / held-weight
status: pending_review
---

# perf_0001 · 压抑隐忍·强度1·内敛·眼神（微表情）

> 强度1 = 痕迹 / 微表情：只露一两个核心 AU、低强度、单区域。微表情常被模型丢失，故本条默认使用「起始帧」模式（峰值静帧兜底），见 `## 起始帧表情`。

## 锁定文本块

> 可逐字嵌入 shot prompt 的 `表情:` / `动作:` 字段。

```
眼眶下缘一闪极淡的泛红、几乎不可察；目光垂向斜下方约半秒、随即收回平视；眉内角极轻一动（忧的痕迹）旋即抚平。无泪、无眨眼变化、嘴部全程不动——情绪只在眼底掠过一瞬，旋即被压回。
```

## 配套镜头

- 景别：特写（眼部占画主体，微表情唯一可辨的景别）。
- 运镜：镜头完全静止；任何运动都会盖过痕迹级微表情。
- 色调：冷调低饱和，平光，避免阴影制造伪表情。

## 起始帧表情

> 本条默认起始帧模式：用下方 Seedream prompt（基于你上传的 actor 参考照 + 峰值表情）生成一张峰值静帧，作 image-to-video 起始帧；检验视频只承担从该帧出发的动作插值。

```
演0001始
[演员: 上传任意 actor 参考照]表演室内，近景特写，正面平视，冷调低饱和、柔和侧光、略压暗（窒息感），9:16。表情：眼眶下缘极淡泛红、泪膜未成形，目光微垂斜下方，眉内角极轻上提，嘴部平静。整体克制、几乎不可察的一瞬忧色。
```

## 检验视频

> 通用 prompt（model-agnostic）：上传你的 actor 参考照（image-to-video，任意演员），场景为表演室、灯光按本情绪氛围。控制变量见 `_performances/_testrig.md`；同一情绪内比较不同 entry 用同一张参考照，唯一变量 = 表演描述。

```
演0001
[演员: 上传任意 actor 参考照，image-to-video]
表演室内，近景，正面平视，镜头静止，冷调低饱和、柔和侧光、略压暗（窒息感），9:16，时长 4 秒。
[起始帧: 本条 ## 起始帧表情 生成的静帧]
表演: 眼眶下缘一闪极淡的泛红、几乎不可察；目光垂向斜下方约半秒、随即收回平视；眉内角极轻一动（忧的痕迹）旋即抚平。无泪、无眨眼变化、嘴部全程不动——情绪只在眼底掠过一瞬，旋即被压回。
```

## 实测与验证

validation_status: pending_review
decision: pending

> 评分：你 + Claude 对这条 prompt 各评一次，每轴 1–5（表演达意 / 情绪可识别 / 是否过火；过火越低越好）。达标 = 表演达意≥4 且 情绪可识别≥4 且 是否过火≤2。合议：双方均达标 ⇒ accept；双方都评过但未同时达标 ⇒ revise（按失败轴改 prompt）；尚有一方未评 ⇒ pending。

| 评分者 | 表演达意 | 情绪可识别 | 是否过火 | 笔记 |
|--------|----------|-----------|----------|------|
| 你 | - | - | - | - |
| Claude | - | - | - | - |

合议: 待评分
